---
phase: 01-a5vm-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/phase1/a5vm_vec_type.mlir
  - test/phase1/a5vm_copy_gm_to_ubuf_op.mlir
  - test/phase1/a5vm_vabs_kernel_shape.mlir
  - test/phase1/a5vm_copy_ubuf_to_gm_op.mlir
  - test/phase1/a5vm_backend_switch.mlir
  - test/phase1/a5vm_shared_dialects.mlir
  - test/phase1/run_phase1_checks.sh
  - test/phase1/a5vm_load_op.mlir
  - test/phase1/a5vm_abs_op.mlir
  - test/phase1/a5vm_store_op.mlir
autonomous: true
requirements:
  - BACK-01
  - BACK-02
  - A5VM-01
  - A5VM-02
  - A5VM-03
  - A5VM-04
must_haves:
  truths:
    - "Phase 1 source fixtures describe only the corrected A5-only backend surface."
    - "Wave 0 no longer treats the Abs path as a5vm.load/a5vm.abs/a5vm.store."
    - "Developers can run one committed Phase 1 runner that checks vector typing, copy primitives, compute-kernel shape, backend selection, and shared-dialect preservation."
  artifacts:
    - path: "test/phase1/a5vm_vec_type.mlir"
      provides: "256-byte a5vm vector type acceptance and rejection cases"
      min_lines: 20
    - path: "test/phase1/a5vm_copy_gm_to_ubuf_op.mlir"
      provides: "GM-to-UB copy primitive assembly and verifier contract"
      min_lines: 20
    - path: "test/phase1/a5vm_vabs_kernel_shape.mlir"
      provides: "vlds/vabs/vsts compute-kernel contract for the Abs path"
      min_lines: 20
    - path: "test/phase1/a5vm_copy_ubuf_to_gm_op.mlir"
      provides: "UB-to-GM copy primitive assembly and verifier contract"
      min_lines: 20
    - path: "test/phase1/a5vm_backend_switch.mlir"
      provides: "explicit --pto-backend=a5vm fixture at the corrected backend seam"
      min_lines: 20
    - path: "test/phase1/a5vm_shared_dialects.mlir"
      provides: "shared func/scf/arith preservation fixture around corrected a5vm primitives"
      min_lines: 20
    - path: "test/phase1/run_phase1_checks.sh"
      provides: "documented direct-runner for the corrected Phase 1 suite"
      min_lines: 20
  key_links:
    - from: "test/phase1/run_phase1_checks.sh"
      to: "test/phase1/a5vm_vabs_kernel_shape.mlir"
      via: "direct ptoas | FileCheck invocation"
      pattern: "a5vm_vabs_kernel_shape\\.mlir"
    - from: "test/phase1/run_phase1_checks.sh"
      to: "test/phase1/a5vm_backend_switch.mlir"
      via: "runner grep guard and corrected backend fixture prove obsolete pseudo-op names were removed"
      pattern: "a5vm_backend_switch\\.mlir|a5vm\\\\\\.\\(load\\|abs\\|store\\)"
---

<objective>
Replace the obsolete Wave 0 contracts with corrected A5-only Phase 1 fixtures.

Purpose: Lock the corrected backend boundary before implementation so every later task targets `mlir::a5vm`, GM/UB copy primitives, and the `vlds` / `vabs` / `vsts` compute shape instead of the obsolete pseudo-op model.
Output: Corrected `test/phase1/*.mlir` fixtures, one executable runner, and removal of the three obsolete pseudo-op fixture files.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-a5vm-foundation/01-CONTEXT.md
@.planning/phases/01-a5vm-foundation/01-RESEARCH.md
@.planning/phases/01-a5vm-foundation/01-VALIDATION.md
@.planning/codebase/TESTING.md
@tools/ptoas/ptoas.cpp
@test/basic/empty_func.mlir
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the Phase 1 MLIR/FileCheck fixtures around corrected A5VM primitives</name>
  <files>test/phase1/a5vm_vec_type.mlir, test/phase1/a5vm_copy_gm_to_ubuf_op.mlir, test/phase1/a5vm_vabs_kernel_shape.mlir, test/phase1/a5vm_copy_ubuf_to_gm_op.mlir, test/phase1/a5vm_backend_switch.mlir, test/phase1/a5vm_shared_dialects.mlir, test/phase1/a5vm_load_op.mlir, test/phase1/a5vm_abs_op.mlir, test/phase1/a5vm_store_op.mlir</files>
  <action>Create the corrected fixture set under `test/phase1/` and remove the three obsolete pseudo-op fixtures. Keep `a5vm_vec_type.mlir`, but update it so the legal cases are still `!a5vm.vec<64xf32>` and `!a5vm.vec<128xi16>` and the illegal cases still fail with diagnostics containing `expected exactly 256 bytes`. Replace the old load/store fixtures with `a5vm_copy_gm_to_ubuf_op.mlir` and `a5vm_copy_ubuf_to_gm_op.mlir`; each must use corrected primitive names close to the A5 builtin layer, explicit metadata for layout, valid rows, valid cols, burst count, burst length, GM stride, UB stride, and UB pad, and at least one negative case showing the verifier rejects the wrong direction or missing metadata. Replace the old unary fixture with `a5vm_vabs_kernel_shape.mlir`; model one minimal UB compute slice using `a5vm.vlds`, `a5vm.vabs`, and `a5vm.vsts` on legal `!a5vm.vec<...>` values, plus one negative case for a mismatched vector/register shape. Rewrite `a5vm_backend_switch.mlir` so `--pto-backend=a5vm -o -` checks for corrected primitives and never for `a5vm.load`, `a5vm.abs`, or `a5vm.store`. Rewrite `a5vm_shared_dialects.mlir` so it proves `func`, `scf`, and `arith` remain visible around corrected A5VM ops. Delete `test/phase1/a5vm_load_op.mlir`, `test/phase1/a5vm_abs_op.mlir`, and `test/phase1/a5vm_store_op.mlir` after the replacement fixtures and checks are in place.</action>
  <verify>
    <automated>bash -lc 'test -f test/phase1/a5vm_vec_type.mlir && test -f test/phase1/a5vm_copy_gm_to_ubuf_op.mlir && test -f test/phase1/a5vm_vabs_kernel_shape.mlir && test -f test/phase1/a5vm_copy_ubuf_to_gm_op.mlir && test -f test/phase1/a5vm_backend_switch.mlir && test -f test/phase1/a5vm_shared_dialects.mlir && test ! -e test/phase1/a5vm_load_op.mlir && test ! -e test/phase1/a5vm_abs_op.mlir && test ! -e test/phase1/a5vm_store_op.mlir && ! rg -n "a5vm\\.(load|abs|store)\\b" test/phase1/a5vm_*.mlir'</automated>
  </verify>
  <done>The committed Phase 1 fixtures only describe corrected A5VM semantics and no longer preserve any pseudo-op contract from the superseded Phase 1 attempt.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the Phase 1 runner for the corrected fixture set</name>
  <files>test/phase1/run_phase1_checks.sh</files>
  <action>Rewrite `test/phase1/run_phase1_checks.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. Resolve `./build/tools/ptoas/ptoas` once, fail loudly if it is missing, and print `phase1 check: <fixture>` before every invocation. Run the corrected suite in this order: vector type, GM-to-UB copy, vabs kernel shape, UB-to-GM copy, backend switch, and shared-dialect preservation. Use direct `ptoas | FileCheck` commands for each fixture and add one explicit grep guard that fails if any Phase 1 fixture still contains `a5vm.load`, `a5vm.abs`, or `a5vm.store`. Keep the runner Phase 1 only; do not patch Phase 2 fixtures in this plan.</action>
  <verify>
    <automated>bash -lc 'test -x test/phase1/run_phase1_checks.sh && rg -n "phase1 check:|a5vm_copy_gm_to_ubuf_op\\.mlir|a5vm_vabs_kernel_shape\\.mlir|a5vm_copy_ubuf_to_gm_op\\.mlir|a5vm_backend_switch\\.mlir|a5vm_shared_dialects\\.mlir" test/phase1/run_phase1_checks.sh && rg -n "a5vm\\\\\\.\\(load\\|abs\\|store\\)" test/phase1/run_phase1_checks.sh'</automated>
  </verify>
  <done>Phase 1 has one executable runner that exercises only the corrected fixture set and actively guards against regression to the obsolete pseudo-op surface.</done>
</task>

</tasks>

<verification>
`bash -lc 'test -x test/phase1/run_phase1_checks.sh && ! rg -n "a5vm\\.(load|abs|store)\\b" test/phase1/*.mlir && rg -n "a5vm_copy_gm_to_ubuf_op\\.mlir|a5vm_vabs_kernel_shape\\.mlir|a5vm_copy_ubuf_to_gm_op\\.mlir" test/phase1/run_phase1_checks.sh'`
</verification>

<success_criteria>
Wave 0 is fully corrected: the Phase 1 contract now names only `mlir::a5vm` vector typing, GM/UB copy primitives, and the `vlds` / `vabs` / `vsts` compute shape, while every obsolete pseudo-op fixture has been removed from the suite.
</success_criteria>

<output>
After completion, create `.planning/phases/01-a5vm-foundation/01-a5vm-foundation-01-SUMMARY.md`
</output>
