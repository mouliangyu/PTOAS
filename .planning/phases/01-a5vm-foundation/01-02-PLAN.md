---
phase: 01-a5vm-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - include/PTO/IR/A5VM.h
  - include/PTO/IR/A5VMDialect.h
  - include/PTO/IR/A5VMDialect.td
  - include/PTO/IR/A5VMOps.td
  - include/PTO/IR/A5VMTypes.td
  - include/PTO/IR/CMakeLists.txt
  - lib/PTO/IR/A5VM.cpp
  - lib/PTO/IR/CMakeLists.txt
  - tools/ptoas/ptoas.cpp
autonomous: true
requirements:
  - A5VM-01
  - A5VM-02
  - A5VM-03
  - A5VM-04
must_haves:
  truths:
    - "Developers can parse and verify legal `!a5vm.vec<...>` types under the corrected `mlir::a5vm` namespace."
    - "Developers can express the Abs path with corrected A5VM primitives close to the A5/CCE builtin layer."
    - "The minimal Phase 1 primitive set covers both GM/UB movement and the register-vector `vlds` / `vabs` / `vsts` compute slice."
  artifacts:
    - path: "include/PTO/IR/A5VMDialect.td"
      provides: "corrected dialect identity and namespace"
      contains: "::mlir::a5vm"
    - path: "include/PTO/IR/A5VMTypes.td"
      provides: "256-byte vector type contract"
      contains: "A5VM_VecType"
    - path: "include/PTO/IR/A5VMOps.td"
      provides: "corrected primitive op surface"
      contains: "def A5VM_VldsOp"
    - path: "lib/PTO/IR/A5VM.cpp"
      provides: "handwritten type/op parsing, printing, and verifier logic"
      min_lines: 140
    - path: "tools/ptoas/ptoas.cpp"
      provides: "corrected dialect registration for fixture parsing"
      contains: "mlir::a5vm::A5VMDialect"
  key_links:
    - from: "include/PTO/IR/A5VMOps.td"
      to: "lib/PTO/IR/A5VM.cpp"
      via: "generated declarations plus handwritten verifiers"
      pattern: "A5VM_(CopyGmToUbuf|Vlds|Vabs|Vsts|CopyUbufToGm)Op"
    - from: "tools/ptoas/ptoas.cpp"
      to: "include/PTO/IR/A5VM.h"
      via: "dialect registration and parse support"
      pattern: "A5VMDialect"
---

<objective>
Replace the obsolete A5VM dialect surface with the corrected `mlir::a5vm` primitive IR.

Purpose: Land the smallest hardware-facing A5VM dialect that can support the future Abs lowering shape without preserving the wrong pseudo-op model.
Output: Corrected A5VM dialect headers, TableGen contracts, handwritten implementation, build hooks, and CLI dialect registration.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-a5vm-foundation/01-CONTEXT.md
@.planning/phases/01-a5vm-foundation/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@include/PTO/IR/PTO.h
@include/PTO/IR/PTODialect.h
@include/PTO/IR/CMakeLists.txt
@lib/PTO/IR/CMakeLists.txt
@lib/PTO/IR/PTO.cpp
@tools/ptoas/ptoas.cpp

<interfaces>
From `tools/ptoas/ptoas.cpp`:
```c++
DialectRegistry registry;
context.getOrLoadDialect<mlir::pto::a5vm::A5VMDialect>();
```

From `include/PTO/Transforms/Passes.h`:
```c++
std::unique_ptr<Pass> createLowerPTOToA5VMPass();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redefine the A5VM dialect contracts around corrected primitives</name>
  <files>include/PTO/IR/A5VM.h, include/PTO/IR/A5VMDialect.h, include/PTO/IR/A5VMDialect.td, include/PTO/IR/A5VMOps.td, include/PTO/IR/A5VMTypes.td, include/PTO/IR/CMakeLists.txt, lib/PTO/IR/CMakeLists.txt</files>
  <action>Rewrite the A5VM dialect contracts so the dialect namespace stays `a5vm` in assembly and the C++ namespace becomes exactly `::mlir::a5vm`. Keep the normalized vector type spelling `!a5vm.vec<...>`. In `A5VMOps.td`, remove the obsolete `a5vm.load`, `a5vm.abs`, and `a5vm.store` contracts and replace them with the corrected minimal primitive set: one GM-to-UB copy primitive, one UB-to-GM copy primitive, and the compute trio `a5vm.vlds`, `a5vm.vabs`, and `a5vm.vsts`. Name the copy ops close to the A5/CCE layer, for example `a5vm.copy_gm_to_ubuf` and `a5vm.copy_ubuf_to_gm`; do not reintroduce PTO-shaped naming. Give the copy-family ops explicit attrs for the Abs slice metadata needed later by Phase 2: layout, valid rows, valid cols, burst count, burst length, GM stride, UB stride, and UB pad. Keep loop/control-flow/scalar behavior out of A5VM entirely. Update the IR CMake files so the corrected dialect, ops, and types are generated and compiled through the standard `PTOIR` path.</action>
  <verify>
    <automated>bash -lc 'rg -n "cppNamespace = \\\"::mlir::a5vm\\\"" include/PTO/IR/A5VMDialect.td && rg -n "def A5VM_(CopyGmToUbuf|Vlds|Vabs|Vsts|CopyUbufToGm)Op" include/PTO/IR/A5VMOps.td && ! rg -n "def A5VM_(Load|Abs|Store)Op" include/PTO/IR/A5VMOps.td && rg -n "A5VM\\.cpp" lib/PTO/IR/CMakeLists.txt'</automated>
  </verify>
  <done>The source tree declares only the corrected minimal A5VM primitive surface and the dialect identity is fixed to `mlir::a5vm`.</done>
</task>

<task type="auto">
  <name>Task 2: Implement corrected A5VM parsing, printing, and verification</name>
  <files>lib/PTO/IR/A5VM.cpp, tools/ptoas/ptoas.cpp, include/PTO/IR/A5VM.h, include/PTO/IR/A5VMDialect.h</files>
  <action>Rewrite `lib/PTO/IR/A5VM.cpp` so the handwritten implementation matches the corrected contracts. Preserve the exact `!a5vm.vec<...>` parse/print form and enforce the 256-byte rule with diagnostics containing `expected exactly 256 bytes`. Verify `vlds` and `vsts` use legal `!a5vm.vec<...>` types around UB-backed buffer access, verify `vabs` is vector-to-vector with identical input and result types, and verify the copy-family ops require the explicit metadata chosen in Task 1. Use the repo’s existing memref/address-space representation to distinguish GM and UB endpoints when that information is already available; do not invent a new A5VM-specific type family in this plan. Update `tools/ptoas/ptoas.cpp` so the corrected dialect is registered and loaded through `mlir::a5vm::A5VMDialect`, not `mlir::pto::a5vm::A5VMDialect`, and raw corrected A5VM fixtures parse cleanly through the CLI.</action>
  <verify>
    <automated>bash -lc './build/tools/ptoas/ptoas test/phase1/a5vm_vec_type.mlir 2>&1 | FileCheck test/phase1/a5vm_vec_type.mlir && ./build/tools/ptoas/ptoas test/phase1/a5vm_copy_gm_to_ubuf_op.mlir -o - | FileCheck test/phase1/a5vm_copy_gm_to_ubuf_op.mlir && ./build/tools/ptoas/ptoas test/phase1/a5vm_vabs_kernel_shape.mlir -o - | FileCheck test/phase1/a5vm_vabs_kernel_shape.mlir && ./build/tools/ptoas/ptoas test/phase1/a5vm_copy_ubuf_to_gm_op.mlir -o - | FileCheck test/phase1/a5vm_copy_ubuf_to_gm_op.mlir'</automated>
  </verify>
  <done>The corrected A5VM dialect parses and verifies the full Phase 1 primitive surface, and every corrected A5VM-specific fixture passes through `ptoas` plus `FileCheck`.</done>
</task>

</tasks>

<verification>
`bash -lc './build/tools/ptoas/ptoas test/phase1/a5vm_vec_type.mlir 2>&1 | FileCheck test/phase1/a5vm_vec_type.mlir && ./build/tools/ptoas/ptoas test/phase1/a5vm_copy_gm_to_ubuf_op.mlir -o - | FileCheck test/phase1/a5vm_copy_gm_to_ubuf_op.mlir && ./build/tools/ptoas/ptoas test/phase1/a5vm_vabs_kernel_shape.mlir -o - | FileCheck test/phase1/a5vm_vabs_kernel_shape.mlir && ./build/tools/ptoas/ptoas test/phase1/a5vm_copy_ubuf_to_gm_op.mlir -o - | FileCheck test/phase1/a5vm_copy_ubuf_to_gm_op.mlir'`
</verification>

<success_criteria>
`mlir::a5vm` exists as a first-class dialect with only the corrected Phase 1 primitive set: legal 256-byte vector typing, GM/UB copy primitives, and the `vlds` / `vabs` / `vsts` compute slice needed for the Abs path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-a5vm-foundation/01-a5vm-foundation-02-SUMMARY.md`
</output>
