---
phase: 01-a5vm-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - include/PTO/Transforms/A5VMTextEmitter.h
  - lib/PTO/Transforms/A5VMTextEmitter.cpp
  - lib/PTO/Transforms/CMakeLists.txt
  - tools/ptoas/ptoas.cpp
autonomous: true
requirements:
  - BACK-01
  - BACK-02
  - A5VM-02
must_haves:
  truths:
    - "Developers can select the corrected A5VM backend through an explicit CLI flag while the EmitC path remains the default."
    - "The corrected backend boundary emits inspectable text from the post-A5VM module instead of routing the A5 branch through the obsolete pseudo-op model."
    - "Shared func/scf/arith structure remains outside A5VM and stays visible when developers inspect the A5VM backend path."
  artifacts:
    - path: "include/PTO/Transforms/A5VMTextEmitter.h"
      provides: "thin corrected-A5VM boundary emission API"
      contains: "translateA5VMModuleToText"
    - path: "lib/PTO/Transforms/A5VMTextEmitter.cpp"
      provides: "corrected backend text emission for the minimal Phase 1 primitive set"
      min_lines: 160
    - path: "tools/ptoas/ptoas.cpp"
      provides: "explicit backend selector and A5VM debug output wiring"
      contains: "--pto-backend"
  key_links:
    - from: "tools/ptoas/ptoas.cpp"
      to: "include/PTO/Transforms/A5VMTextEmitter.h"
      via: "final backend branch at the current emit boundary"
      pattern: "translateA5VMModuleToText"
    - from: "lib/PTO/Transforms/A5VMTextEmitter.cpp"
      to: "test/phase1/a5vm_backend_switch.mlir"
      via: "corrected primitive names in backend text output"
      pattern: "a5vm\\.(copy_gm_to_ubuf|vlds|vabs|vsts|copy_ubuf_to_gm)"
---

<objective>
Correct the backend boundary so the A5VM branch uses the new primitive surface without redesigning the pass pipeline.

Purpose: Put the corrected `--pto-backend=a5vm` seam in place at the current EmitC boundary while keeping shared dialect structure intact and visible.
Output: Corrected backend text-emission helper, updated CLI wiring, and a passing backend-switch/shared-dialect contract for the corrected A5VM surface.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-a5vm-foundation/01-CONTEXT.md
@.planning/phases/01-a5vm-foundation/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@tools/ptoas/ptoas.cpp
@include/PTO/Transforms/Passes.h
@include/PTO/Transforms/A5VMTextEmitter.h
@lib/PTO/Transforms/A5VMTextEmitter.cpp
@lib/PTO/Transforms/PTOToEmitC.cpp
@test/phase1/a5vm_backend_switch.mlir
@test/phase1/a5vm_shared_dialects.mlir
@test/phase1/run_phase1_checks.sh

<interfaces>
From `include/PTO/Transforms/Passes.h`:
```c++
enum class PTOArch {
  A3,
  A5,
};

std::unique_ptr<Pass> createEmitPTOManualPass(PTOArch arch);
std::unique_ptr<Pass> createLowerPTOToA5VMPass();
```

From `tools/ptoas/ptoas.cpp`:
```c++
pm.addPass(pto::createEmitPTOManualPass(pto::PTOArch::A5));
if (failed(emitc::translateToCpp(*module, cppOS, declareVariablesAtTop)))
  return 1;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the thin A5VM text emitter around corrected primitives</name>
  <files>include/PTO/Transforms/A5VMTextEmitter.h, lib/PTO/Transforms/A5VMTextEmitter.cpp, lib/PTO/Transforms/CMakeLists.txt</files>
  <action>Rewrite the existing `A5VMTextEmitter` helper so it no longer assumes `a5vm.load`, `a5vm.abs`, or `a5vm.store`. Keep this helper Phase 1-thin: it should emit readable backend text for the corrected minimal primitive set and preserve shared `func`, `scf`, and `arith` structure instead of trying to finalize full Phase 3 HIVM naming. The emitted text must reflect corrected A5VM ops such as the GM/UB copy family and the `vlds` / `vabs` / `vsts` compute slice. If an op reaches this seam that is not part of the corrected Phase 1 surface, emit an explicit diagnostic or placeholder text that names the unsupported op instead of guessing or silently falling back to obsolete behavior. Update `lib/PTO/Transforms/CMakeLists.txt` so the helper is built through the normal transforms library.</action>
  <verify>
    <automated>bash -lc 'rg -n "a5vm\\.(copy_gm_to_ubuf|vlds|vabs|vsts|copy_ubuf_to_gm)" lib/PTO/Transforms/A5VMTextEmitter.cpp && ! rg -n "a5vm\\.(load|abs|store)\\b" lib/PTO/Transforms/A5VMTextEmitter.cpp include/PTO/Transforms/A5VMTextEmitter.h && rg -n "A5VMTextEmitter\\.cpp" lib/PTO/Transforms/CMakeLists.txt'</automated>
  </verify>
  <done>The backend text helper only understands the corrected primitive surface and makes unsupported cases explicit instead of hiding them behind the superseded pseudo-op model.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the corrected backend selector at the existing emission seam</name>
  <files>tools/ptoas/ptoas.cpp</files>
  <action>Update `tools/ptoas/ptoas.cpp` so `--pto-backend=emitc|a5vm` remains explicit, defaults to `emitc`, and routes the A5VM branch through the corrected backend boundary instead of `emitc::translateToCpp`. Keep the upstream pass pipeline shape intact. On the `a5vm` branch, run the shared pre-backend passes, run PTO-to-A5VM only for that branch, and then send the resulting module to `translateA5VMModuleToText(...)`. Keep raw corrected-A5VM textual fixtures parseable for Phase 1 tests, and keep `--a5vm-print-ir` as the developer-visible inspection flag so shared `func` / `scf` / `arith` operations remain inspectable around corrected A5VM ops. Do not add any dependency on `a2a3`, and do not reintroduce pseudo-op names anywhere in the backend switch path.</action>
  <verify>
    <automated>bash -lc './build/tools/ptoas/ptoas --pto-backend=a5vm test/phase1/a5vm_backend_switch.mlir -o - | FileCheck test/phase1/a5vm_backend_switch.mlir && ./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase1/a5vm_shared_dialects.mlir -o /dev/null 2>&1 | FileCheck test/phase1/a5vm_shared_dialects.mlir'</automated>
  </verify>
  <done>`ptoas` exposes a corrected `--pto-backend=a5vm` path at the current backend seam, keeps EmitC as the default backend, and emits inspectable corrected A5VM output without pulling shared control flow or scalar arithmetic into the dialect.</done>
</task>

</tasks>

<verification>
`bash -lc 'bash test/phase1/run_phase1_checks.sh && ./build/tools/ptoas/ptoas --pto-backend=a5vm test/phase1/a5vm_backend_switch.mlir -o - | FileCheck test/phase1/a5vm_backend_switch.mlir && ./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase1/a5vm_shared_dialects.mlir -o /dev/null 2>&1 | FileCheck test/phase1/a5vm_shared_dialects.mlir'`
</verification>

<success_criteria>
The corrected A5VM backend entry point exists at the current EmitC seam, is selected explicitly with `--pto-backend=a5vm`, emits readable corrected-A5VM text for the Phase 1 primitive set, and leaves shared `func` / `scf` / `arith` structure outside the dialect.
</success_criteria>

<output>
After completion, create `.planning/phases/01-a5vm-foundation/01-a5vm-foundation-03-SUMMARY.md`
</output>
