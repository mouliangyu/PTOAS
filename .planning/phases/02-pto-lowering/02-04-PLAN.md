---
phase: 02-pto-lowering
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/PTO/Transforms/PTOToA5VMLowering.cpp
  - test/phase2/tload_copy_family_shape.mlir
  - test/phase2/tstore_copy_family_shape.mlir
autonomous: true
requirements:
  - PTO-01
  - PTO-03
  - PTO-04
gap_closure: true
must_haves:
  truths:
    - "Lowered `a5vm.copy_gm_to_ubuf` shows concrete `src_strides`, `trace_offsets`, and `trace_sizes` instead of dynamic sentinels or empty arrays."
    - "Lowered `a5vm.copy_ubuf_to_gm` shows concrete `dst_strides`, `trace_offsets`, and `trace_sizes` instead of dynamic sentinels or empty arrays."
    - "The Phase 2 copy-family fixtures lock exact metadata recovery through the real memref/subview form that reaches lowering."
  artifacts:
    - path: "lib/PTO/Transforms/PTOToA5VMLowering.cpp"
      provides: "trace and stride recovery through partition-view plus memref/subview chains"
      min_lines: 300
    - path: "test/phase2/tload_copy_family_shape.mlir"
      provides: "fixture locking exact TLOAD stride and trace metadata"
      min_lines: 20
    - path: "test/phase2/tstore_copy_family_shape.mlir"
      provides: "fixture locking exact TSTORE stride and trace metadata"
      min_lines: 20
  key_links:
    - from: "lib/PTO/Transforms/PTOToA5VMLowering.cpp"
      to: "test/phase2/tload_copy_family_shape.mlir"
      via: "resolved partition offsets, sizes, and tensor-view strides attached to `a5vm.copy_gm_to_ubuf`"
      pattern: "trace_offsets|trace_sizes|src_strides"
    - from: "lib/PTO/Transforms/PTOToA5VMLowering.cpp"
      to: "test/phase2/tstore_copy_family_shape.mlir"
      via: "resolved partition offsets, sizes, and tensor-view strides attached to `a5vm.copy_ubuf_to_gm`"
      pattern: "trace_offsets|trace_sizes|dst_strides"
---

<objective>
Close the copy-family metadata gap for `TLOAD` and `TSTORE`.

Purpose: Recover the exact stride and partition-trace facts that the verifier identified as missing from observable lowered IR so backend selection inputs are preserved instead of degraded to sentinel values.
Output: Updated metadata extraction in the lowering helper plus tightened TLOAD/TSTORE fixtures that lock the exact recovered attrs.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pto-lowering/02-CONTEXT.md
@.planning/phases/02-pto-lowering/02-RESEARCH.md
@.planning/phases/02-pto-lowering/02-VERIFICATION.md
@.planning/phases/02-pto-lowering/02-pto-lowering-02-SUMMARY.md
@include/PTO/Transforms/A5VMLowering.h
@lib/PTO/Transforms/PTOToA5VMLowering.cpp
@test/phase2/tload_copy_family_shape.mlir
@test/phase2/tstore_copy_family_shape.mlir

<interfaces>
From `include/PTO/Transforms/A5VMLowering.h`:
```c++
struct A5VMPartitionTrace {
  SmallVector<int64_t> offsets;
  SmallVector<int64_t> sizes;
  bool hasDynamicOffsets = false;
  bool hasDynamicSizes = false;
};

struct A5VMLoadContract {
  SmallVector<int64_t> sourceStrides;
  A5VMPartitionTrace trace;
};

struct A5VMStoreContract {
  SmallVector<int64_t> destinationStrides;
  A5VMPartitionTrace trace;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recover exact partition trace and tensor-view strides through the lowering-time value chain</name>
  <read_first>lib/PTO/Transforms/PTOToA5VMLowering.cpp, include/PTO/Transforms/A5VMLowering.h, .planning/phases/02-pto-lowering/02-VERIFICATION.md</read_first>
  <files>lib/PTO/Transforms/PTOToA5VMLowering.cpp</files>
  <action>Update `lib/PTO/Transforms/PTOToA5VMLowering.cpp` so the copy-family contracts survive the actual IR shape that reaches Phase 2 lowering. Fix `extractPartitionTrace` so it can recover offsets and sizes when the lowering input is a `memref.subview`, `memref.reinterpret_cast`, or `memref.cast` wrapped around a `pto.partition_view`, not only when `PartitionViewOp` is the direct producer. Fix `resolveTensorViewBase` so `TLOAD` and `TSTORE` recover concrete tensor-view strides from the original `pto.make_tensor_view` path even after the shared pre-backend rewrites insert memref adapters. Keep the recovery logic directional: `TLOAD` must fill `sourceStrides`, and `TSTORE` must fill `destinationStrides`. Do not paper over missing data by emitting new sentinel constants or by hardcoding `[32, 1]`; the code must traverse the producer chain and reuse the actual shape/stride/trace facts.</action>
  <acceptance_criteria>Running `./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir` against both copy-family fixtures now produces lowered IR that satisfies the committed FileCheck contracts for concrete stride and partition-trace metadata, instead of printing dynamic sentinel strides or empty trace arrays.</acceptance_criteria>
  <verify>
    <automated>bash -lc 'filecheck_bin="$(command -v FileCheck || command -v FileCheck-19 || true)"; if [[ -z "${filecheck_bin}" && -x /usr/lib/llvm-19/bin/FileCheck ]]; then filecheck_bin=/usr/lib/llvm-19/bin/FileCheck; fi; test -n "${filecheck_bin}" && test -x "${filecheck_bin}" && ./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase2/tload_copy_family_shape.mlir -o /dev/null 2>&1 | "${filecheck_bin}" test/phase2/tload_copy_family_shape.mlir && ./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase2/tstore_copy_family_shape.mlir -o /dev/null 2>&1 | "${filecheck_bin}" test/phase2/tstore_copy_family_shape.mlir'</automated>
  </verify>
  <done>The lowering helper can recover exact copy-family stride and trace metadata from the value graph that actually reaches Phase 2 lowering, without degrading to dynamic sentinels or empty arrays.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the exact recovered metadata in the copy-family fixtures</name>
  <read_first>test/phase2/tload_copy_family_shape.mlir, test/phase2/tstore_copy_family_shape.mlir, .planning/phases/02-pto-lowering/02-VERIFICATION.md, lib/PTO/Transforms/PTOToA5VMLowering.cpp</read_first>
  <files>test/phase2/tload_copy_family_shape.mlir, test/phase2/tstore_copy_family_shape.mlir</files>
  <action>Update the two copy-family fixtures so they lock the exact metadata required by the verification gaps. `test/phase2/tload_copy_family_shape.mlir` must explicitly require `src_strides = [32, 1]`, `trace_offsets = [0, 0]`, and `trace_sizes = [32, 32]` on the lowered `a5vm.copy_gm_to_ubuf`. `test/phase2/tstore_copy_family_shape.mlir` must explicitly require `dst_strides = [32, 1]`, `trace_offsets = [0, 0]`, and `trace_sizes = [32, 32]` on the lowered `a5vm.copy_ubuf_to_gm`. Keep the existing loop-programming checks in place so the semantic fix stays tied to the copy-family structure rather than to attrs alone.</action>
  <acceptance_criteria>The fixture contracts are only considered complete if their real `ptoas --a5vm-print-ir` output passes FileCheck and therefore proves the committed metadata checks describe emitted IR, not just fixture text.</acceptance_criteria>
  <verify>
    <automated>bash -lc 'filecheck_bin="$(command -v FileCheck || command -v FileCheck-19 || true)"; if [[ -z "${filecheck_bin}" && -x /usr/lib/llvm-19/bin/FileCheck ]]; then filecheck_bin=/usr/lib/llvm-19/bin/FileCheck; fi; test -n "${filecheck_bin}" && test -x "${filecheck_bin}" && ./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase2/tload_copy_family_shape.mlir -o /dev/null 2>&1 | "${filecheck_bin}" test/phase2/tload_copy_family_shape.mlir && ./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase2/tstore_copy_family_shape.mlir -o /dev/null 2>&1 | "${filecheck_bin}" test/phase2/tstore_copy_family_shape.mlir'</automated>
  </verify>
  <done>The committed copy-family fixtures now fail unless observable lowered IR preserves the exact stride and partition-trace metadata required for backend code selection, as proven by runtime `ptoas | FileCheck` execution.</done>
</task>

</tasks>

<verification>
`bash -lc 'filecheck_bin="$(command -v FileCheck || command -v FileCheck-19 || true)"; if [[ -z "${filecheck_bin}" && -x /usr/lib/llvm-19/bin/FileCheck ]]; then filecheck_bin=/usr/lib/llvm-19/bin/FileCheck; fi; test -n "${filecheck_bin}" && test -x "${filecheck_bin}" && ./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase2/tload_copy_family_shape.mlir -o /dev/null 2>&1 | "${filecheck_bin}" test/phase2/tload_copy_family_shape.mlir && ./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase2/tstore_copy_family_shape.mlir -o /dev/null 2>&1 | "${filecheck_bin}" test/phase2/tstore_copy_family_shape.mlir'`
</verification>

<success_criteria>
Phase 2 no longer drops exact copy-family selection metadata: both TLOAD and TSTORE preserve concrete strides and partition traces in actual lowered IR, and the committed fixtures prove it through runtime `ptoas | FileCheck` checks.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pto-lowering/02-pto-lowering-04-SUMMARY.md`
</output>
