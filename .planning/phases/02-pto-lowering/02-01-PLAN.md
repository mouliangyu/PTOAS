---
phase: 02-pto-lowering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - include/PTO/Transforms/A5VMLowering.h
  - test/phase2/tload_copy_family_shape.mlir
  - test/phase2/tabs_abs_loop_shape.mlir
  - test/phase2/tabs_precheck_a5.mlir
  - test/phase2/tstore_copy_family_shape.mlir
  - test/phase2/tstore_domain_todos.mlir
  - test/phase2/pto_backend_a5vm_wiring.mlir
  - test/phase2/run_phase2_checks.sh
autonomous: true
requirements:
  - PTO-01
  - PTO-02
  - PTO-03
  - PTO-04
must_haves:
  truths:
    - "Phase 2 fixtures reject the superseded plain-`scf.for` interpretation of `__VEC_SCOPE__` and instead require an explicit AIV vector-scope carrier attached to the chosen loop."
    - "The public lowering contract names `lowerTLOAD`, `lowerTABS`, and `lowerTSTORE` and makes the AIV loop-scope carrier visible before helper implementation starts."
    - "Developers can verify TLOAD copy-family structure, TABS `vlds -> vabs -> vsts` structure plus AIV loop ownership, TSTORE copy-family structure, and backend branch wiring from committed Phase 2 fixtures."
  artifacts:
    - path: "include/PTO/Transforms/A5VMLowering.h"
      provides: "stable public contracts for copy-family lowering and explicit AIV loop-metadata carriage"
      contains: "A5VMLoopScopeContract"
    - path: "test/phase2/tabs_abs_loop_shape.mlir"
      provides: "fixture that identifies which lowered loop carries AIV vector-scope semantics"
      min_lines: 20
    - path: "test/phase2/run_phase2_checks.sh"
      provides: "Phase 2 runner for the corrected fixture set"
      min_lines: 20
  key_links:
    - from: "include/PTO/Transforms/A5VMLowering.h"
      to: "test/phase2/tabs_abs_loop_shape.mlir"
      via: "shared contract names for the AIV carrier used by the TABS fixture and later lowering helper"
      pattern: "A5VMLoopScopeContract|llvm\\.loop\\.aivector_scope|cce_aiv_loop_hint"
    - from: "test/phase2/run_phase2_checks.sh"
      to: "test/phase2/tabs_abs_loop_shape.mlir"
      via: "runner executes the vec-scope fixture and rejects obsolete pseudo-op semantics"
      pattern: "tabs_abs_loop_shape\\.mlir|a5vm\\.(load|store|abs)"
---

<objective>
Lock the corrected Phase 2 execution contract before implementation starts.

Purpose: Replace the superseded Phase 2 assumptions with a Wave 1 contract that preserves PTO template correspondence, exposes a stable carrier for AIV loop metadata, and forces the later implementation to keep `__VEC_SCOPE__` on an explicitly identified loop instead of treating it as plain loop structure.
Output: A rewritten public lowering contract header, corrected MLIR/FileCheck fixtures, and an updated Phase 2 runner.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pto-lowering/02-CONTEXT.md
@.planning/phases/02-pto-lowering/02-RESEARCH.md
@.planning/phases/02-pto-lowering/02-VALIDATION.md
@.planning/phases/01-a5vm-foundation/01-CONTEXT.md
@include/PTO/IR/A5VMOps.td
@include/PTO/IR/PTOOps.td
@include/PTO/Transforms/A5VMLowering.h
@tools/ptoas/ptoas.cpp

<interfaces>
From `include/PTO/Transforms/A5VMLowering.h`:
```c++
struct A5VMLoadContract;
struct A5VMUnaryContract;
struct A5VMStoreContract;
LogicalResult lowerTLOAD(TLoadOp op, PatternRewriter &rewriter);
LogicalResult lowerTABS(TAbsOp op, PatternRewriter &rewriter);
LogicalResult lowerTSTORE(TStoreOp op, PatternRewriter &rewriter);
```

From `include/PTO/IR/A5VMOps.td`:
```tablegen
def A5VM_CopyGmToUbufOp;
def A5VM_CopyUbufToGmOp;
def A5VM_VldsOp;
def A5VM_VabsOp;
def A5VM_VstsOp;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the Phase 2 contract surface with an explicit AIV loop carrier</name>
  <read_first>include/PTO/Transforms/A5VMLowering.h, include/PTO/IR/PTOOps.td, .planning/phases/02-pto-lowering/02-CONTEXT.md, .planning/phases/02-pto-lowering/02-RESEARCH.md</read_first>
  <files>include/PTO/Transforms/A5VMLowering.h</files>
  <action>Rewrite `include/PTO/Transforms/A5VMLowering.h` so the public Phase 2 contract makes the corrected `__VEC_SCOPE__` meaning impossible to miss. Keep the public entrypoints named exactly `lowerTLOAD`, `lowerTABS`, and `lowerTSTORE`. Preserve the existing copy-family contract structs for `TLOAD` and `TSTORE`, but add an explicit loop-metadata carrier for unary lowering using concrete names: `enum class A5VMLoopScopeKind { None, AIVVectorScope };`, `struct A5VMLoopScopeContract { A5VMLoopScopeKind kind = A5VMLoopScopeKind::None; StringRef sourceAttr = \"cce_aiv_loop_hint\"; StringRef loweredAttr = \"llvm.loop.aivector_scope\"; int64_t loopDepth = 0; };`, and add that struct as a field on the unary contract so `lowerTABS` can preserve which loop carries AIV semantics separately from ordinary chunking loops. Declare helper APIs with concrete names that later plans must implement: `attachLoopScopeMetadata`, `programCopyGmToUbLoops`, `programCopyUbToGmLoops`, and `buildUnaryVecScope`. Do not leave any contract wording that implies plain `scf.for` structure alone represents `__VEC_SCOPE__`.</action>
  <acceptance_criteria>`include/PTO/Transforms/A5VMLowering.h` contains the exact strings `enum class A5VMLoopScopeKind`, `AIVVectorScope`, `struct A5VMLoopScopeContract`, `sourceAttr = "cce_aiv_loop_hint"`, `loweredAttr = "llvm.loop.aivector_scope"`, `attachLoopScopeMetadata`, `lowerTLOAD`, `lowerTABS`, and `lowerTSTORE`.</acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "enum class A5VMLoopScopeKind|AIVVectorScope|struct A5VMLoopScopeContract|sourceAttr = \\\"cce_aiv_loop_hint\\\"|loweredAttr = \\\"llvm\\.loop\\.aivector_scope\\\"|attachLoopScopeMetadata|lowerTLOAD|lowerTABS|lowerTSTORE" include/PTO/Transforms/A5VMLowering.h'</automated>
  </verify>
  <done>The public Phase 2 interface exposes a stable AIV loop-metadata carrier and the three PTO entrypoints without relying on plain structural loop nesting as the semantic representation of `__VEC_SCOPE__`.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the Wave 1 fixtures and runner around explicit AIV loop ownership</name>
  <read_first>test/phase2/tload_copy_family_shape.mlir, test/phase2/tabs_abs_loop_shape.mlir, test/phase2/tabs_precheck_a5.mlir, test/phase2/tstore_copy_family_shape.mlir, test/phase2/tstore_domain_todos.mlir, test/phase2/pto_backend_a5vm_wiring.mlir, test/phase2/run_phase2_checks.sh, include/PTO/Transforms/A5VMLowering.h, .planning/phases/02-pto-lowering/02-VALIDATION.md</read_first>
  <files>test/phase2/tload_copy_family_shape.mlir, test/phase2/tabs_abs_loop_shape.mlir, test/phase2/tabs_precheck_a5.mlir, test/phase2/tstore_copy_family_shape.mlir, test/phase2/tstore_domain_todos.mlir, test/phase2/pto_backend_a5vm_wiring.mlir, test/phase2/run_phase2_checks.sh</files>
  <action>Rewrite the Phase 2 fixtures so they lock the corrected semantics. `test/phase2/tload_copy_family_shape.mlir` must continue checking GM-to-UB copy-family lowering and visible `set_loop*_outtoub` programming. `test/phase2/tstore_copy_family_shape.mlir` must continue checking UB-to-GM copy-family lowering and visible `set_loop*_ubtoout` programming. `test/phase2/tstore_domain_todos.mlir` must keep explicit `VEC`, `ACC`, and `MAT` branch coverage with dedicated TODO diagnostics for `ACC` and `MAT`. `test/phase2/tabs_precheck_a5.mlir` must keep pre-A5 validation coverage. The key correction is in `test/phase2/tabs_abs_loop_shape.mlir`: add checks for an explicit AIV scope carrier on the chosen lowered loop, using exact strings `cce_aiv_loop_hint` and `llvm.loop.aivector_scope`, while still checking ordered `a5vm.vlds`, `a5vm.vabs`, and `a5vm.vsts`. The runner `test/phase2/run_phase2_checks.sh` must execute the revised fixture set, print `phase2 check:` lines for each file, and fail if any fixture still depends on `a5vm.load`, `a5vm.store`, `a5vm.abs`, or checks only bare `scf.for` nesting without the AIV carrier strings.</action>
  <acceptance_criteria>`test/phase2/tabs_abs_loop_shape.mlir` contains the exact strings `cce_aiv_loop_hint`, `llvm.loop.aivector_scope`, `a5vm.vlds`, `a5vm.vabs`, and `a5vm.vsts`; `test/phase2/run_phase2_checks.sh` contains the fixture names `tload_copy_family_shape.mlir`, `tabs_abs_loop_shape.mlir`, `tabs_precheck_a5.mlir`, `tstore_copy_family_shape.mlir`, `tstore_domain_todos.mlir`, and `pto_backend_a5vm_wiring.mlir`; no Phase 2 fixture contains the strings `a5vm.load`, `a5vm.store`, or `a5vm.abs`.</acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "cce_aiv_loop_hint|llvm\\.loop\\.aivector_scope|a5vm\\.vlds|a5vm\\.vabs|a5vm\\.vsts" test/phase2/tabs_abs_loop_shape.mlir && rg -n "tload_copy_family_shape\\.mlir|tabs_abs_loop_shape\\.mlir|tabs_precheck_a5\\.mlir|tstore_copy_family_shape\\.mlir|tstore_domain_todos\\.mlir|pto_backend_a5vm_wiring\\.mlir" test/phase2/run_phase2_checks.sh && ! rg -n "a5vm\\.(load|store|abs)\\b" test/phase2/*.mlir'</automated>
  </verify>
  <done>The Phase 2 fixture suite and runner now enforce explicit AIV vector-scope ownership for `TABS` while preserving the corrected copy-family and backend-wiring contracts for `TLOAD` and `TSTORE`.</done>
</task>

</tasks>

<verification>
`bash -lc 'rg -n "A5VMLoopScopeContract|llvm\\.loop\\.aivector_scope" include/PTO/Transforms/A5VMLowering.h test/phase2/tabs_abs_loop_shape.mlir && ! rg -n "a5vm\\.(load|store|abs)\\b" test/phase2/*.mlir && rg -n "phase2 check:" test/phase2/run_phase2_checks.sh'`
</verification>

<success_criteria>
Wave 1 leaves no ambiguity: the public Phase 2 contract and fixture suite both require an explicit carrier for AIV loop metadata, and the remaining lowering work is forced to preserve which `TABS` loop owns `__VEC_SCOPE__`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pto-lowering/02-pto-lowering-01-SUMMARY.md`
</output>
