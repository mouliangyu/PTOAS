---
phase: 02-pto-lowering
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/PTO/Transforms/PTOToA5VMLowering.cpp
  - lib/PTO/Transforms/CMakeLists.txt
autonomous: true
requirements:
  - PTO-01
  - PTO-02
  - PTO-03
  - PTO-04
must_haves:
  truths:
    - "The helper layer preserves copy-family PTO template structure for `TLOAD` and `TSTORE` instead of collapsing them into pseudo load/store ops."
    - "The helper layer lowers `TABS` through `vlds -> vabs -> vsts` while attaching explicit AIV vector-scope metadata to the chosen loop."
    - "Phase 2 exposes a stable loop-metadata carrier that Phase 3 can read during textual HIVM emission."
  artifacts:
    - path: "lib/PTO/Transforms/PTOToA5VMLowering.cpp"
      provides: "truthful lowering helpers for copy-family and unary lowering with explicit loop metadata"
      min_lines: 260
    - path: "lib/PTO/Transforms/CMakeLists.txt"
      provides: "build integration for the corrected helper implementation"
      contains: "PTOToA5VMLowering.cpp"
  key_links:
    - from: "lib/PTO/Transforms/PTOToA5VMLowering.cpp"
      to: "include/PTO/Transforms/A5VMLowering.h"
      via: "helper implementation must realize the explicit `A5VMLoopScopeContract` interface"
      pattern: "A5VMLoopScopeContract|attachLoopScopeMetadata|buildUnaryVecScope"
    - from: "lib/PTO/Transforms/PTOToA5VMLowering.cpp"
      to: "test/phase2/tabs_abs_loop_shape.mlir"
      via: "helper emits the ordered unary vector pipeline and loop metadata locked by the fixture"
      pattern: "llvm\\.loop\\.aivector_scope|cce_aiv_loop_hint|a5vm::(VldsOp|VabsOp|VstsOp)"
---

<objective>
Implement the corrected lowering helper layer that preserves PTO structure and explicit AIV loop scope.

Purpose: Realize the public contracts from Wave 1 so `lowerTLOAD`, `lowerTABS`, and `lowerTSTORE` stay recognizable as PTO template decompositions while exposing a stable carrier for later HIVM emission.
Output: A build-wired `PTOToA5VMLowering.cpp` that preserves copy-family loop programming, branch structure, and explicit AIV loop metadata.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pto-lowering/02-CONTEXT.md
@.planning/phases/02-pto-lowering/02-RESEARCH.md
@include/PTO/Transforms/A5VMLowering.h
@include/PTO/IR/A5VM.h
@lib/PTO/Transforms/PTOToA5VMLowering.cpp
@lib/PTO/Transforms/CMakeLists.txt
@lib/PTO/Transforms/PTOToEmitC.cpp
@test/phase2/tload_copy_family_shape.mlir
@test/phase2/tabs_abs_loop_shape.mlir
@test/phase2/tstore_copy_family_shape.mlir
@test/phase2/tstore_domain_todos.mlir

<interfaces>
From `include/PTO/Transforms/A5VMLowering.h`:
```c++
enum class A5VMLoopScopeKind { None, AIVVectorScope };
struct A5VMLoopScopeContract;
LogicalResult attachLoopScopeMetadata(Operation *loopLikeOp,
                                      const A5VMLoopScopeContract &contract,
                                      Builder &builder);
LogicalResult buildUnaryVecScope(StringRef family,
                                 const A5VMUnaryContract &contract, Value src,
                                 Value dst, PatternRewriter &rewriter,
                                 Location loc);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement truthful copy-family lowering helpers for TLOAD and TSTORE</name>
  <read_first>lib/PTO/Transforms/PTOToA5VMLowering.cpp, include/PTO/Transforms/A5VMLowering.h, lib/PTO/Transforms/PTOToEmitC.cpp, test/phase2/tload_copy_family_shape.mlir, test/phase2/tstore_copy_family_shape.mlir, test/phase2/tstore_domain_todos.mlir, .planning/phases/02-pto-lowering/02-RESEARCH.md</read_first>
  <files>lib/PTO/Transforms/PTOToA5VMLowering.cpp</files>
  <action>Rework `lib/PTO/Transforms/PTOToA5VMLowering.cpp` so the TLOAD/TSTORE portions read like PTO library decompositions, not pseudo memory ops. Keep helper extraction for layout, shape, stride, valid-region, partition-trace, pad/init, and tile-domain inputs. Materialize concrete copy-family lowering through `a5vm::CopyGmToUbufOp` and `a5vm::CopyUbufToGmOp`, then apply explicit loop-programming helpers named `programCopyGmToUbLoops` and `programCopyUbToGmLoops` so the generated IR retains `set_loop*_outtoub` and `set_loop*_ubtoout` semantics. Keep explicit `VEC`, `ACC`, and `MAT` source-domain branching for `TSTORE`; for `ACC` and `MAT`, emit dedicated TODO diagnostics with the exact phrases `TSTORE ACC lowering TODO` and `TSTORE MAT lowering TODO` instead of generic failure text. Do not introduce any helper named `lowerLoadLike`, `lowerStoreLike`, or any path that creates `a5vm.load`/`a5vm.store`.</action>
  <acceptance_criteria>`lib/PTO/Transforms/PTOToA5VMLowering.cpp` contains the exact strings `programCopyGmToUbLoops`, `programCopyUbToGmLoops`, `a5vm::CopyGmToUbufOp`, `a5vm::CopyUbufToGmOp`, `TSTORE ACC lowering TODO`, and `TSTORE MAT lowering TODO`; the file does not contain the strings `a5vm.load`, `a5vm.store`, `lowerLoadLike`, or `lowerStoreLike`.</acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "programCopyGmToUbLoops|programCopyUbToGmLoops|a5vm::CopyGmToUbufOp|a5vm::CopyUbufToGmOp|TSTORE ACC lowering TODO|TSTORE MAT lowering TODO" lib/PTO/Transforms/PTOToA5VMLowering.cpp && ! rg -n "a5vm\\.load|a5vm\\.store|lowerLoadLike|lowerStoreLike" lib/PTO/Transforms/PTOToA5VMLowering.cpp'</automated>
  </verify>
  <done>The helper layer preserves TLOAD/TSTORE copy-family semantics, loop programming, and explicit TSTORE domain branching in a form future PTO ops can reuse.</done>
</task>

<task type="auto">
  <name>Task 2: Implement unary TABS lowering with explicit AIV loop metadata and build integration</name>
  <read_first>lib/PTO/Transforms/PTOToA5VMLowering.cpp, lib/PTO/Transforms/CMakeLists.txt, include/PTO/Transforms/A5VMLowering.h, test/phase2/tabs_abs_loop_shape.mlir, test/phase2/tabs_precheck_a5.mlir, .planning/phases/02-pto-lowering/02-RESEARCH.md</read_first>
  <files>lib/PTO/Transforms/PTOToA5VMLowering.cpp, lib/PTO/Transforms/CMakeLists.txt</files>
  <action>Implement the unary portion of `lib/PTO/Transforms/PTOToA5VMLowering.cpp` so `lowerTABS` performs A5-side prechecks before creating any A5VM IR, then builds the unary structure as two separate concerns: a chosen loop that carries `A5VMLoopScopeKind::AIVVectorScope` metadata through `attachLoopScopeMetadata`, and any surrounding plain software chunking loop required for UB iteration. The chosen AIV loop must carry both source-of-truth names from the contract, `cce_aiv_loop_hint` and `llvm.loop.aivector_scope`, so Phase 3 can recover the intended carrier during textual emission. Inside the relevant loop body, build the ordered primitive sequence `a5vm::VldsOp`, `a5vm::VabsOp`, and `a5vm::VstsOp`. Update `lib/PTO/Transforms/CMakeLists.txt` so this helper translation unit remains part of `PTOTransforms` after the rewrite. Do not leave `__VEC_SCOPE__` represented only by plain `scf.for` nesting without an attached carrier.</action>
  <acceptance_criteria>`lib/PTO/Transforms/PTOToA5VMLowering.cpp` contains the exact strings `A5VMLoopScopeKind::AIVVectorScope`, `attachLoopScopeMetadata`, `cce_aiv_loop_hint`, `llvm.loop.aivector_scope`, `a5vm::VldsOp`, `a5vm::VabsOp`, and `a5vm::VstsOp`; `lib/PTO/Transforms/CMakeLists.txt` contains the string `PTOToA5VMLowering.cpp`.</acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "A5VMLoopScopeKind::AIVVectorScope|attachLoopScopeMetadata|cce_aiv_loop_hint|llvm\\.loop\\.aivector_scope|a5vm::VldsOp|a5vm::VabsOp|a5vm::VstsOp" lib/PTO/Transforms/PTOToA5VMLowering.cpp && rg -n "PTOToA5VMLowering\\.cpp" lib/PTO/Transforms/CMakeLists.txt'</automated>
  </verify>
  <done>The helper layer now preserves which `TABS` loop carries AIV vector-scope semantics and exposes that information in a stable, Phase-3-readable form while keeping the ordered unary primitive pipeline visible.</done>
</task>

</tasks>

<verification>
`bash -lc 'rg -n "llvm\\.loop\\.aivector_scope|a5vm::VldsOp|a5vm::VabsOp|a5vm::VstsOp" lib/PTO/Transforms/PTOToA5VMLowering.cpp && rg -n "PTOToA5VMLowering\\.cpp" lib/PTO/Transforms/CMakeLists.txt'`
</verification>

<success_criteria>
Wave 2 produces a truthful helper layer: copy-family lowering remains PTO-template-shaped, `TABS` carries explicit AIV loop metadata, and the resulting structure is stable enough for later textual HIVM emission.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pto-lowering/02-pto-lowering-02-SUMMARY.md`
</output>
