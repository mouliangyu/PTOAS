---
phase: 02-pto-lowering
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - include/PTO/Transforms/Passes.h
  - include/PTO/Transforms/Passes.td
  - lib/PTO/Transforms/PTOToA5VM.cpp
  - tools/ptoas/ptoas.cpp
autonomous: true
requirements:
  - PTO-02
  - PTO-04
must_haves:
  truths:
    - "Running `ptoas --pto-backend=a5vm` lowers PTO ops through the corrected helper layer and preserves the explicit AIV loop-metadata carrier for `TABS`."
    - "The registered pass architecture stays reusable for later PTO ops and Phase 3 textual HIVM emission."
    - "The backend branch does not regress to emitter-side recovery or obsolete pseudo A5VM ops."
  artifacts:
    - path: "include/PTO/Transforms/Passes.td"
      provides: "registered `pto-to-a5vm` pass contract with required dependent dialects"
      contains: "pto-to-a5vm"
    - path: "lib/PTO/Transforms/PTOToA5VM.cpp"
      provides: "pass dispatch that routes only through `lowerTLOAD`, `lowerTABS`, and `lowerTSTORE`"
      min_lines: 120
    - path: "tools/ptoas/ptoas.cpp"
      provides: "A5VM backend branch that schedules PTO-to-A5VM before text emission and keeps debug IR visible"
      contains: "createLowerPTOToA5VMPass"
  key_links:
    - from: "lib/PTO/Transforms/PTOToA5VM.cpp"
      to: "lib/PTO/Transforms/PTOToA5VMLowering.cpp"
      via: "pass dispatch must preserve the helper-produced AIV carrier instead of rebuilding TABS ad hoc"
      pattern: "lowerTLOAD|lowerTABS|lowerTSTORE"
    - from: "tools/ptoas/ptoas.cpp"
      to: "test/phase2/pto_backend_a5vm_wiring.mlir"
      via: "backend branch must make the corrected A5VM lowering observable to the fixture suite"
      pattern: "createLowerPTOToA5VMPass|a5vm-print-ir"
---

<objective>
Register and wire the corrected PTO-to-A5VM execution path.

Purpose: Turn the corrected helper layer into the actual `ptoas --pto-backend=a5vm` behavior while preserving the explicit AIV loop carrier for later textual HIVM emission and keeping Phase 2 debug IR faithful to PTO template structure.
Output: Pass registration, pass dispatch, and CLI backend wiring that execute the corrected Phase 2 lowering path.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pto-lowering/02-CONTEXT.md
@.planning/phases/02-pto-lowering/02-RESEARCH.md
@include/PTO/Transforms/A5VMLowering.h
@include/PTO/Transforms/Passes.h
@include/PTO/Transforms/Passes.td
@lib/PTO/Transforms/PTOToA5VM.cpp
@lib/PTO/Transforms/PTOToA5VMLowering.cpp
@tools/ptoas/ptoas.cpp
@test/phase2/pto_backend_a5vm_wiring.mlir
@test/phase2/run_phase2_checks.sh

<interfaces>
From `include/PTO/Transforms/A5VMLowering.h`:
```c++
LogicalResult lowerTLOAD(TLoadOp op, PatternRewriter &rewriter);
LogicalResult lowerTABS(TAbsOp op, PatternRewriter &rewriter);
LogicalResult lowerTSTORE(TStoreOp op, PatternRewriter &rewriter);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register `pto-to-a5vm` as a thin pass boundary over the corrected helper layer</name>
  <read_first>include/PTO/Transforms/Passes.h, include/PTO/Transforms/Passes.td, lib/PTO/Transforms/PTOToA5VM.cpp, include/PTO/Transforms/A5VMLowering.h, lib/PTO/Transforms/PTOToA5VMLowering.cpp</read_first>
  <files>include/PTO/Transforms/Passes.h, include/PTO/Transforms/Passes.td, lib/PTO/Transforms/PTOToA5VM.cpp</files>
  <action>Update the Phase 2 pass boundary so it remains thin and reusable. `include/PTO/Transforms/Passes.h` and `include/PTO/Transforms/Passes.td` must keep the pass name `pto-to-a5vm` and declare the dependent dialects needed by the corrected lowering path, including the dialects required to preserve helper-built loop metadata and `scf` structure. In `lib/PTO/Transforms/PTOToA5VM.cpp`, keep the pass focused on legality, pattern registration, and failure propagation only: match `TLoadOp`, `TAbsOp`, and `TStoreOp`, then call `lowerTLOAD`, `lowerTABS`, and `lowerTSTORE`. Do not rebuild unary loop structure here, do not emit `emitc::CallOpaqueOp`, and do not create any `a5vm.load`, `a5vm.store`, or single-op `a5vm.abs` path.</action>
  <acceptance_criteria>`include/PTO/Transforms/Passes.h` or `include/PTO/Transforms/Passes.td` contains the exact strings `pto-to-a5vm` and `createLowerPTOToA5VMPass`; `lib/PTO/Transforms/PTOToA5VM.cpp` contains the exact strings `lowerTLOAD(`, `lowerTABS(`, and `lowerTSTORE(`; `lib/PTO/Transforms/PTOToA5VM.cpp` does not contain the strings `emitc::CallOpaqueOp`, `a5vm.load`, `a5vm.store`, or `a5vm.abs`.</acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "pto-to-a5vm|createLowerPTOToA5VMPass" include/PTO/Transforms/Passes.h include/PTO/Transforms/Passes.td && rg -n "lowerTLOAD\\(|lowerTABS\\(|lowerTSTORE\\(" lib/PTO/Transforms/PTOToA5VM.cpp && ! rg -n "emitc::CallOpaqueOp|a5vm\\.load|a5vm\\.store|a5vm\\.abs" lib/PTO/Transforms/PTOToA5VM.cpp'</automated>
  </verify>
  <done>The registered pass remains a clean conversion boundary that preserves the helper-produced semantics and stays reusable for future PTO ops.</done>
</task>

<task type="auto">
  <name>Task 2: Wire `ptoas --pto-backend=a5vm` through the corrected lowering path and fixture-visible IR</name>
  <read_first>tools/ptoas/ptoas.cpp, test/phase2/pto_backend_a5vm_wiring.mlir, test/phase2/run_phase2_checks.sh, .planning/phases/02-pto-lowering/02-VALIDATION.md, lib/PTO/Transforms/PTOToA5VM.cpp</read_first>
  <files>tools/ptoas/ptoas.cpp</files>
  <action>Update `tools/ptoas/ptoas.cpp` so the A5VM backend path runs the shared pre-backend passes, then `createLowerPTOToA5VMPass()`, then the existing A5VM text-emission seam. Preserve `--a5vm-print-ir` so the Wave 1 fixtures can observe the helper-produced copy-family and unary structure before final text emission. Keep the A5VM branch separate from the EmitC branch, and make sure the lowered TABS structure that reaches debug IR still exposes whichever loop carries the AIV metadata from Wave 2 rather than flattening that distinction away. Do not route A5VM through `createEmitPTOManualPass(...)`, emitter-side semantic recovery, or any fallback that erases the explicit AIV carrier before Phase 3.</action>
  <acceptance_criteria>`tools/ptoas/ptoas.cpp` contains the exact strings `createLowerPTOToA5VMPass`, `a5vm-print-ir`, and `PTOBackend::A5VM`; the file does not contain any A5VM branch call to `createEmitPTOManualPass`; the backend wiring fixture `test/phase2/pto_backend_a5vm_wiring.mlir` remains named in `test/phase2/run_phase2_checks.sh`.</acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "createLowerPTOToA5VMPass|a5vm-print-ir|PTOBackend::A5VM" tools/ptoas/ptoas.cpp && ! rg -n "PTOBackend::A5VM[[:space:][:print:]]*createEmitPTOManualPass" tools/ptoas/ptoas.cpp && rg -n "pto_backend_a5vm_wiring\\.mlir" test/phase2/run_phase2_checks.sh'</automated>
  </verify>
  <done>`ptoas --pto-backend=a5vm` is wired to the corrected lowering path, keeps debug IR observable for the fixture suite, and preserves the explicit AIV loop carrier needed by later HIVM emission.</done>
</task>

</tasks>

<verification>
`bash -lc 'CCACHE_DISABLE=1 ninja -C build PTOTransforms ptoas && bash test/phase2/run_phase2_checks.sh'`
</verification>

<success_criteria>
Wave 3 makes the corrected plan executable: the pass is registered, `ptoas` runs it on the A5VM branch, and the resulting IR path preserves explicit AIV loop semantics instead of degrading `__VEC_SCOPE__` to plain structural loops.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pto-lowering/02-pto-lowering-03-SUMMARY.md`
</output>
