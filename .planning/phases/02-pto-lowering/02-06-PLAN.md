---
phase: 02-pto-lowering
plan: 06
type: execute
wave: 3
depends_on:
  - 02-05
files_modified:
  - test/phase2/run_phase2_checks.sh
  - .planning/phases/02-pto-lowering/deferred-items.md
  - .planning/phases/02-pto-lowering/02-VALIDATION.md
autonomous: true
requirements:
  - PTO-01
  - PTO-02
  - PTO-03
  - PTO-04
gap_closure: true
must_haves:
  truths:
    - "Developers can invoke the committed Phase 2 verification entrypoint in this workspace without an immediate stale-fixture failure."
    - "The runner has an explicit FileCheck resolution strategy instead of assuming `FileCheck` is on `PATH`."
    - "If FileCheck is unavailable, the runner exits with a clear actionable error instead of a shell `command not found` failure."
  artifacts:
    - path: "test/phase2/run_phase2_checks.sh"
      provides: "workspace-runnable Phase 2 verification entrypoint with FileCheck discovery"
      min_lines: 35
    - path: ".planning/phases/02-pto-lowering/02-VALIDATION.md"
      provides: "updated validation commands matching the runnable entrypoint"
      contains: "run_phase2_checks.sh"
    - path: ".planning/phases/02-pto-lowering/deferred-items.md"
      provides: "updated blocker log after the runner mismatch is removed"
      min_lines: 1
  key_links:
    - from: "test/phase2/run_phase2_checks.sh"
      to: "test/phase2/tabs_abs_loop_shape.mlir"
      via: "preflight now enforces the corrected vec-scope contract without rejecting the committed fixture by mistake"
      pattern: "tabs_abs_loop_shape\\.mlir|llvm\\.loop\\.aivector_scope|cce_aiv_loop_hint"
    - from: "test/phase2/run_phase2_checks.sh"
      to: ".planning/phases/02-pto-lowering/02-VALIDATION.md"
      via: "validation docs name the same runnable entrypoint and FileCheck strategy"
      pattern: "FileCheck|run_phase2_checks\\.sh"
---

<objective>
Make the committed Phase 2 verification entrypoint runnable in this workspace.

Purpose: Remove the stale fixture/runner mismatch and define a concrete FileCheck-path strategy so the committed verification script becomes a reliable entrypoint after the semantic fixes land.
Output: An updated runner, aligned validation docs, and a cleaned-up deferred-items note that no longer reports the stale preflight mismatch as an active blocker.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pto-lowering/02-VALIDATION.md
@.planning/phases/02-pto-lowering/02-VERIFICATION.md
@.planning/phases/02-pto-lowering/deferred-items.md
@test/phase2/run_phase2_checks.sh
@test/phase2/tabs_abs_loop_shape.mlir
@test/phase2/tload_copy_family_shape.mlir
@test/phase2/tabs_precheck_a5.mlir
@test/phase2/tstore_copy_family_shape.mlir
@test/phase2/tstore_domain_todos.mlir
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a workspace-safe FileCheck discovery path to the Phase 2 runner</name>
  <read_first>test/phase2/run_phase2_checks.sh, .planning/phases/02-pto-lowering/02-VERIFICATION.md, .planning/phases/02-pto-lowering/02-VALIDATION.md</read_first>
  <files>test/phase2/run_phase2_checks.sh</files>
  <action>Update `test/phase2/run_phase2_checks.sh` so the committed runner resolves `FileCheck` explicitly instead of assuming it is on `PATH`. Prefer a short deterministic search order suitable for this workspace, for example `FileCheck`, `FileCheck-19`, then `/usr/lib/llvm-19/bin/FileCheck` if present. Store the result in a named variable such as `filecheck_bin` and invoke that variable for every fixture. If none of the candidates exist, exit with a clear error message naming the checked paths. Keep the existing `phase2 check:` output lines and the existing stale-surface guards.</action>
  <acceptance_criteria>With the build tree present, `bash test/phase2/run_phase2_checks.sh` runs the committed Phase 2 suite end to end using the runner's resolved `filecheck_bin` path rather than a bare PATH-only `FileCheck` assumption. If no candidate `FileCheck` exists, the runner exits early with a clear actionable message naming the checked locations.</acceptance_criteria>
  <verify>
    <automated>bash -lc 'bash test/phase2/run_phase2_checks.sh'</automated>
  </verify>
  <done>The runner can locate FileCheck in the current workspace and execute the committed checks, or fail with a deterministic actionable message instead of an implicit PATH-dependent shell error.</done>
</task>

<task type="auto">
  <name>Task 2: Align validation docs and deferred notes with the runnable runner contract</name>
  <read_first>.planning/phases/02-pto-lowering/02-VALIDATION.md, .planning/phases/02-pto-lowering/deferred-items.md, test/phase2/run_phase2_checks.sh, .planning/phases/02-pto-lowering/02-VERIFICATION.md</read_first>
  <files>.planning/phases/02-pto-lowering/02-VALIDATION.md, .planning/phases/02-pto-lowering/deferred-items.md</files>
  <action>Update `.planning/phases/02-pto-lowering/02-VALIDATION.md` so its quick-run and full-suite commands match the new runner reality and describe the FileCheck resolution strategy instead of a bare PATH assumption. Update `.planning/phases/02-pto-lowering/deferred-items.md` to remove the stale `tabs_abs_loop_shape.mlir` preflight mismatch once the committed runner and fixture agree, while keeping any remaining independent build-tree blockers that are still real. Do not silently delete the broader generated-header blocker unless this plan actually fixes it.</action>
  <acceptance_criteria>The validation docs name the same runnable entrypoint that is exercised by `bash test/phase2/run_phase2_checks.sh`, and the deferred note only keeps blockers that still remain after that runner smoke. The documented quick/full commands and the committed runner behavior no longer diverge on FileCheck resolution.</acceptance_criteria>
  <verify>
    <automated>bash -lc 'filecheck_bin="$(command -v FileCheck || command -v FileCheck-19 || true)"; if [[ -z "${filecheck_bin}" && -x /usr/lib/llvm-19/bin/FileCheck ]]; then filecheck_bin=/usr/lib/llvm-19/bin/FileCheck; fi; rg -n "run_phase2_checks\\.sh|filecheck_bin|FileCheck-19|/usr/lib/llvm-19/bin/FileCheck" .planning/phases/02-pto-lowering/02-VALIDATION.md && ! rg -n "tabs_abs_loop_shape\\.mlir still contains bare // CHECK: scf\\.for lines" .planning/phases/02-pto-lowering/deferred-items.md && [[ -n "${filecheck_bin}" ]]'</automated>
  </verify>
  <done>The planning docs match the runnable Phase 2 entrypoint, and the deferred blocker log only mentions still-real blockers instead of the stale fixture/runner mismatch that this plan resolves.</done>
</task>

</tasks>

<verification>
`bash -lc 'bash test/phase2/run_phase2_checks.sh'`
</verification>

<success_criteria>
The committed Phase 2 runner becomes a real workspace entrypoint: `bash test/phase2/run_phase2_checks.sh` is the primary verification path, it no longer trips over a stale fixture preflight, and it has an explicit FileCheck availability strategy that matches this repository.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pto-lowering/02-pto-lowering-06-SUMMARY.md`
</output>
