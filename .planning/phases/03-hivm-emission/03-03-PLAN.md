---
phase: 03-hivm-emission
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - include/PTO/Transforms/A5VMTextEmitter.h
  - lib/PTO/Transforms/A5VMTextEmitter.cpp
  - lib/PTO/Transforms/CMakeLists.txt
autonomous: true
requirements:
  - HIVM-01
  - HIVM-03
must_haves:
  truths:
    - "The Abs subset can be rendered as an LLVM-like module with declarations, definitions, LLVM vector types, and call sites rather than EmitC/C++."
    - "Unresolved mappings stay visible in the main text as structured placeholder calls while a sidecar report explains the missing selection inputs."
    - "The printer stays limited to the implemented subset and shared control-flow/scalar forms instead of redesigning the whole pass pipeline."
  artifacts:
    - path: "include/PTO/Transforms/A5VMTextEmitter.h"
      provides: "text-emission API and options contract"
      contains: "A5VMEmissionOptions"
    - path: "lib/PTO/Transforms/A5VMTextEmitter.cpp"
      provides: "LLVM-like module printer and unresolved-report serializer"
      min_lines: 220
    - path: "lib/PTO/Transforms/CMakeLists.txt"
      provides: "build integration for the text emitter"
      contains: "A5VMTextEmitter.cpp"
  key_links:
    - from: "lib/PTO/Transforms/A5VMTextEmitter.cpp"
      to: "lib/PTO/Transforms/HIVMIntrinsicNaming.cpp"
      via: "printer requests IntrinsicSelection instead of hand-building names"
      pattern: "selectIntrinsic"
    - from: "lib/PTO/Transforms/A5VMTextEmitter.cpp"
      to: "test/phase3/abs_emit.mlir"
      via: "module/declaration/function/call shape matched by the fixture"
      pattern: "ModuleID|declare|define void|call"
---

<objective>
Implement the LLVM-like text printer and unresolved-report serialization on top of the naming layer.

Purpose: Produce a final textual HIVM artifact for the implemented subset while preserving placeholder structure and a machine-readable unresolved inventory for later intrinsic confirmation.
Output: A dedicated `A5VMTextEmitter` API and implementation that emits module text and optional unresolved sidecar records.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hivm-emission/03-CONTEXT.md
@.planning/phases/03-hivm-emission/03-RESEARCH.md
@.planning/phases/03-hivm-emission/03-VALIDATION.md
@.planning/phases/03-hivm-emission/03-02-PLAN.md
@tools/ptoas/ptoas.cpp
@lib/PTO/Transforms/CMakeLists.txt
@test/phase3/backend_switch.mlir
@test/phase3/unresolved_report.mlir
@test/phase3/abs_emit.mlir

<interfaces>
From `include/PTO/Transforms/HIVMIntrinsicNaming.h`:
```c++
struct IntrinsicSelection;
struct UnresolvedEmissionRecord;
FailureOr<IntrinsicSelection> selectIntrinsic(Operation *op);
```

From `.planning/phases/03-hivm-emission/03-RESEARCH.md`:
```c++
struct A5VMEmissionOptions {
  bool dumpA5VMIR = false;
  bool printIntrinsicSelections = false;
  bool allowUnresolved = false;
  std::string unresolvedReportPath;
};
LogicalResult translateA5VMModuleToText(ModuleOp module,
                                        llvm::raw_ostream &irOS,
                                        llvm::raw_ostream &diagOS,
                                        const A5VMEmissionOptions &options);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the A5VM text-emission API</name>
  <read_first>
    include/PTO/Transforms/A5VMTextEmitter.h
    .planning/phases/03-hivm-emission/03-RESEARCH.md
    .planning/phases/03-hivm-emission/03-02-PLAN.md
    test/phase3/backend_switch.mlir
    test/phase3/unresolved_report.mlir
  </read_first>
  <files>include/PTO/Transforms/A5VMTextEmitter.h</files>
  <action>Create `include/PTO/Transforms/A5VMTextEmitter.h` with `struct A5VMEmissionOptions` and `LogicalResult translateA5VMModuleToText(ModuleOp module, llvm::raw_ostream &irOS, llvm::raw_ostream &diagOS, const A5VMEmissionOptions &options);`. The options contract must cover at least `dumpA5VMIR`, `printIntrinsicSelections`, and `unresolvedReportPath`; if an explicit unresolved-tolerance boolean is kept from earlier work, document how it interacts with placeholder emission rather than leaving the policy implicit. Keep the contract printer-focused: it should accept a post-pass `ModuleOp` and produce `.ll`-style text plus optional diagnostics, not add new pass entrypoints or pipeline mutations.</action>
  <acceptance_criteria>
    rg -n 'A5VMEmissionOptions|translateA5VMModuleToText|dumpA5VMIR|printIntrinsicSelections|unresolvedReportPath' include/PTO/Transforms/A5VMTextEmitter.h
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f include/PTO/Transforms/A5VMTextEmitter.h &amp;&amp; rg -n "A5VMEmissionOptions|translateA5VMModuleToText" include/PTO/Transforms/A5VMTextEmitter.h'</automated>
  </verify>
  <done>The repository contains a dedicated printer API whose inputs and options match the final-emission boundary required by Phase 3.</done>
</task>

<task type="auto">
  <name>Task 2: Implement LLVM-like module printing and unresolved sidecar emission</name>
  <read_first>
    lib/PTO/Transforms/A5VMTextEmitter.cpp
    include/PTO/Transforms/A5VMTextEmitter.h
    include/PTO/Transforms/HIVMIntrinsicNaming.h
    lib/PTO/Transforms/CMakeLists.txt
    test/phase3/backend_switch.mlir
    test/phase3/unresolved_report.mlir
    test/phase3/abs_emit.mlir
  </read_first>
  <files>lib/PTO/Transforms/A5VMTextEmitter.cpp, lib/PTO/Transforms/CMakeLists.txt</files>
  <action>Create `lib/PTO/Transforms/A5VMTextEmitter.cpp` and add it to `PTOTransforms`. Implement a printer that emits a complete LLVM-like module: `; ModuleID =`, deduplicated `declare` lines for resolved or placeholder callees, `define` blocks for the implemented functions, LLVM-style vector types such as `<64 x float>`, and call sites selected through `selectIntrinsic(...)`. Emit shared control-flow and simple scalar/vector forms appearing on the Abs subset as LLVM-like text instead of mixed A5VM syntax. When selection is unresolved, keep the call in the main output using a non-reserved placeholder callee and serialize a machine-readable sidecar record containing the source op, placeholder or candidate name, used fields, missing fields, and location. Keep report serialization separate from stdout so normal output remains clean, and do not reuse EmitC cleanup or C++ marker rewrite logic from `PTOToEmitC.cpp`.</action>
  <acceptance_criteria>
    rg -n 'translateA5VMModuleToText|ModuleID|declare|define void|selectIntrinsic' lib/PTO/Transforms/A5VMTextEmitter.cpp
    rg -n '__ptoas_hivm_unresolved|usedFields|missingFields|candidateName|unresolvedReportPath' lib/PTO/Transforms/A5VMTextEmitter.cpp
    rg -n 'A5VMTextEmitter\\.cpp' lib/PTO/Transforms/CMakeLists.txt
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'ninja -C build PTOTransforms ptoas &amp;&amp; report=$(mktemp) &amp;&amp; out=$(mktemp) &amp;&amp; trap "rm -f \"$report\" \"$out\"" EXIT &amp;&amp; ./build/tools/ptoas/ptoas --pto-backend=a5vm --hivm-unresolved-report="$report" test/phase3/unresolved_report.mlir -o - | FileCheck test/phase3/unresolved_report.mlir &amp;&amp; FileCheck --check-prefix=REPORT test/phase3/unresolved_report.mlir &lt; "$report" &amp;&amp; ./build/tools/ptoas/ptoas --pto-backend=a5vm test/phase3/abs_emit.mlir -o "$out" &amp;&amp; /data/mouliangyu/projects/github.com/llvm/llvm-project/install/bin/llvm-as "$out" -o /dev/null &amp;&amp; FileCheck test/phase3/abs_emit.mlir &lt; "$out"'</automated>
  </verify>
  <done>The project builds with a dedicated text emitter that can render the implemented subset as an LLVM-like module, preserve unresolved calls structurally, and persist sidecar report records without depending on EmitC output machinery.</done>
</task>

</tasks>

<verification>
`bash -lc 'ninja -C build PTOTransforms ptoas && report=$(mktemp) && out=$(mktemp) && trap "rm -f \"$report\" \"$out\"" EXIT && ./build/tools/ptoas/ptoas --pto-backend=a5vm --hivm-unresolved-report="$report" test/phase3/unresolved_report.mlir -o - | FileCheck test/phase3/unresolved_report.mlir && FileCheck --check-prefix=REPORT test/phase3/unresolved_report.mlir < "$report" && ./build/tools/ptoas/ptoas --pto-backend=a5vm test/phase3/abs_emit.mlir -o "$out" && /data/mouliangyu/projects/github.com/llvm/llvm-project/install/bin/llvm-as "$out" -o /dev/null && FileCheck test/phase3/abs_emit.mlir < "$out"'`
</verification>

<success_criteria>
Phase 3 has a standalone text emitter that prints a complete LLVM-like module for the implemented subset, consumes the shared naming layer, and records unresolved selections in a separate machine-readable artifact instead of comments only.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hivm-emission/03-hivm-emission-03-SUMMARY.md`
</output>
