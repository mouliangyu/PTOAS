---
phase: 03-hivm-emission
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - include/PTO/Transforms/HIVMIntrinsicNaming.h
  - lib/PTO/Transforms/HIVMIntrinsicNaming.cpp
  - lib/PTO/Transforms/CMakeLists.txt
autonomous: true
requirements:
  - HIVM-02
  - HIVM-03
must_haves:
  truths:
    - "Intrinsic spelling decisions are computed from op family, element type, vector shape, and variant metadata instead of being embedded inside the printer."
    - "Unsupported or incomplete naming cases produce structured unresolved records that keep the call-site shape recoverable."
    - "The naming layer is reusable for the Abs subset without freezing a one-off hardcoded path."
  artifacts:
    - path: "include/PTO/Transforms/HIVMIntrinsicNaming.h"
      provides: "shared naming contracts and unresolved-record types"
      contains: "IntrinsicSelection"
    - path: "lib/PTO/Transforms/HIVMIntrinsicNaming.cpp"
      provides: "family-specific name builders and unresolved-selection logic"
      min_lines: 180
    - path: "lib/PTO/Transforms/CMakeLists.txt"
      provides: "build integration for the naming implementation"
      contains: "HIVMIntrinsicNaming.cpp"
  key_links:
    - from: "lib/PTO/Transforms/HIVMIntrinsicNaming.cpp"
      to: "include/PTO/Transforms/HIVMIntrinsicNaming.h"
      via: "selectIntrinsic and family builder implementations"
      pattern: "selectIntrinsic|selectLoadIntrinsic|selectUnaryIntrinsic|selectStoreIntrinsic"
    - from: "lib/PTO/Transforms/HIVMIntrinsicNaming.cpp"
      to: "test/phase3/intrinsic_naming.mlir"
      via: "family/type/variant fragments matched by the Wave 0 fixture"
      pattern: "vld|vabs|vst|__ptoas_hivm_unresolved"
---

<objective>
Create the reusable HIVM intrinsic-name synthesis layer before printer and CLI work.

Purpose: Isolate naming policy from text printing so Phase 3 satisfies the locked requirement that op kind, type shape, and variant metadata drive intrinsic spellings and unresolved diagnostics.
Output: A shared naming contract header, implementation for load/store/unary families, and build wiring for the helper library.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hivm-emission/03-CONTEXT.md
@.planning/phases/03-hivm-emission/03-RESEARCH.md
@.planning/phases/03-hivm-emission/03-VALIDATION.md
@.planning/phases/01-a5vm-foundation/01-03-PLAN.md
@.planning/phases/02-pto-lowering/02-03-PLAN.md
@include/PTO/Transforms/Passes.h
@lib/PTO/Transforms/CMakeLists.txt
@lib/PTO/Transforms/PTOToEmitC.cpp
@test/phase3/intrinsic_naming.mlir
@test/phase3/unresolved_report.mlir

<interfaces>
From `.planning/phases/01-a5vm-foundation/01-03-PLAN.md`:
```c++
struct A5VMEmissionOptions {
  bool printIntrinsicSelections;
  bool allowUnresolved;
  std::string unresolvedReportPath;
};
LogicalResult translateA5VMModuleToText(ModuleOp module,
                                        llvm::raw_ostream &os,
                                        const A5VMEmissionOptions &options,
                                        llvm::raw_ostream &diagOS);
```

From `.planning/phases/02-pto-lowering/02-03-PLAN.md`:
```c++
// Lowered a5vm ops are expected to carry attrs such as:
// load: layout, src_shape, src_strides, tile_layout, tile_domain,
//       valid_rows, valid_cols, trace_offsets, trace_sizes
// abs:  unary_family = "abs", tile_domain, tile_layout, valid_rows, valid_cols
// store: src_domain, dst_layout, dst_shape, dst_strides,
//        valid_rows, valid_cols, trace_offsets, trace_sizes
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shared naming and unresolved-report contracts</name>
  <read_first>
    include/PTO/Transforms/HIVMIntrinsicNaming.h
    .planning/phases/03-hivm-emission/03-CONTEXT.md
    .planning/phases/03-hivm-emission/03-RESEARCH.md
    .planning/phases/01-a5vm-foundation/01-03-PLAN.md
    .planning/phases/02-pto-lowering/02-03-PLAN.md
    test/phase3/intrinsic_naming.mlir
    test/phase3/unresolved_report.mlir
  </read_first>
  <files>include/PTO/Transforms/HIVMIntrinsicNaming.h</files>
  <action>Create `include/PTO/Transforms/HIVMIntrinsicNaming.h` with a naming-focused public contract, not printer code. Declare structs for `IntrinsicSelection`, `NamingInputs`, and `UnresolvedEmissionRecord`. The contract must carry: source op name, selected callee name, placeholder name, candidate name, used naming fields, missing naming fields, result type fragment, and source location string. Declare reusable entrypoints `FailureOr<IntrinsicSelection> selectIntrinsic(Operation *op);`, `FailureOr<IntrinsicSelection> selectLoadIntrinsic(...)`, `FailureOr<IntrinsicSelection> selectUnaryIntrinsic(...)`, and `FailureOr<IntrinsicSelection> selectStoreIntrinsic(...)`. Make the header comments explicit that names are synthesized from op family, element type, vector shape, and variant fields, and that unresolved cases must keep a machine-distinguishable placeholder under the non-reserved `__ptoas_hivm_unresolved` namespace rather than `llvm.`.</action>
  <acceptance_criteria>
    rg -n 'IntrinsicSelection|NamingInputs|UnresolvedEmissionRecord|selectIntrinsic|selectLoadIntrinsic|selectUnaryIntrinsic|selectStoreIntrinsic' include/PTO/Transforms/HIVMIntrinsicNaming.h
    rg -n '__ptoas_hivm_unresolved|usedFields|missingFields|candidateName|placeholderName' include/PTO/Transforms/HIVMIntrinsicNaming.h
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f include/PTO/Transforms/HIVMIntrinsicNaming.h &amp;&amp; rg -n "IntrinsicSelection|UnresolvedEmissionRecord|selectIntrinsic" include/PTO/Transforms/HIVMIntrinsicNaming.h'</automated>
  </verify>
  <done>The repository contains a standalone naming contract that fixes the boundary between intrinsic selection, unresolved bookkeeping, and later text printing.</done>
</task>

<task type="auto">
  <name>Task 2: Implement family-specific intrinsic-name builders</name>
  <read_first>
    lib/PTO/Transforms/HIVMIntrinsicNaming.cpp
    lib/PTO/Transforms/CMakeLists.txt
    include/PTO/Transforms/HIVMIntrinsicNaming.h
    lib/PTO/Transforms/PTOToEmitC.cpp
    test/phase3/intrinsic_naming.mlir
    test/phase3/unresolved_report.mlir
  </read_first>
  <files>lib/PTO/Transforms/HIVMIntrinsicNaming.cpp, lib/PTO/Transforms/CMakeLists.txt</files>
  <action>Create `lib/PTO/Transforms/HIVMIntrinsicNaming.cpp` and add it to `PTOTransforms`. Implement shared fragment helpers for element-type and vector-shape spelling, plus separate selection builders for `load/store` and unary families. The logic must consume A5VM attrs when present and derive missing pieces from operand/result type shape where possible; do not duplicate every naming input as an attribute if the type already encodes it. For resolved cases, emit callee names that assemble family fragments and vector/type fragments consistently. For unresolved cases, return an `IntrinsicSelection` that includes a non-reserved placeholder name, candidate name when known, used fields, missing fields, and a useful location string. When selection fails, expose which naming fields were unsupported or absent instead of returning only a boolean failure. Keep the printer out of this file; this plan is only about selection logic and structured reporting inputs.</action>
  <acceptance_criteria>
    rg -n 'selectIntrinsic|selectLoadIntrinsic|selectUnaryIntrinsic|selectStoreIntrinsic' lib/PTO/Transforms/HIVMIntrinsicNaming.cpp
    rg -n 'usedFields|missingFields|candidateName|placeholderName|__ptoas_hivm_unresolved' lib/PTO/Transforms/HIVMIntrinsicNaming.cpp
    rg -n 'HIVMIntrinsicNaming\\.cpp' lib/PTO/Transforms/CMakeLists.txt
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'ninja -C build PTOTransforms ptoas &amp;&amp; ./build/tools/ptoas/ptoas --pto-backend=a5vm --dump-a5vm-ir test/phase3/intrinsic_naming.mlir -o - 2&gt;&amp;1 | FileCheck test/phase3/intrinsic_naming.mlir &amp;&amp; report=$(mktemp) &amp;&amp; trap "rm -f \"$report\"" EXIT &amp;&amp; ./build/tools/ptoas/ptoas --pto-backend=a5vm --hivm-unresolved-report="$report" test/phase3/unresolved_report.mlir -o - | FileCheck test/phase3/unresolved_report.mlir &amp;&amp; FileCheck --check-prefix=REPORT test/phase3/unresolved_report.mlir &lt; "$report"'</automated>
  </verify>
  <done>The project builds with a dedicated intrinsic-selection helper layer whose resolved and unresolved outputs are rich enough for the later printer and report sidecar to consume without re-deriving semantics.</done>
</task>

</tasks>

<verification>
`bash -lc 'ninja -C build PTOTransforms ptoas && ./build/tools/ptoas/ptoas --pto-backend=a5vm --dump-a5vm-ir test/phase3/intrinsic_naming.mlir -o - 2>&1 | FileCheck test/phase3/intrinsic_naming.mlir && report=$(mktemp) && trap "rm -f \"$report\"" EXIT && ./build/tools/ptoas/ptoas --pto-backend=a5vm --hivm-unresolved-report="$report" test/phase3/unresolved_report.mlir -o - | FileCheck test/phase3/unresolved_report.mlir && FileCheck --check-prefix=REPORT test/phase3/unresolved_report.mlir < "$report"'`
</verification>

<success_criteria>
Phase 3 has a reusable intrinsic-selection layer that centralizes family/type/variant naming decisions, preserves unresolved metadata explicitly, and can be consumed by a later text printer without embedding naming policy in output formatting code.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hivm-emission/03-hivm-emission-02-SUMMARY.md`
</output>
