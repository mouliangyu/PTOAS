---
phase: 03-hivm-emission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/phase3/backend_switch.mlir
  - test/phase3/intrinsic_naming.mlir
  - test/phase3/unresolved_report.mlir
  - test/phase3/abs_emit.mlir
  - test/phase3/run_phase3_checks.sh
  - .planning/phases/03-hivm-emission/03-VALIDATION.md
autonomous: true
requirements:
  - HIVM-01
  - HIVM-02
  - HIVM-03
must_haves:
  truths:
    - "Developers can run committed source fixtures that lock the exact HIVM text shape, naming behavior, and unresolved-report behavior before emitter work starts."
    - "Each Phase 3 requirement has a concrete automated command instead of a Wave 0 placeholder."
    - "The phase validation path proves the final emission boundary with FileCheck plus local `llvm-as` parsing rather than human inspection alone."
  artifacts:
    - path: "test/phase3/backend_switch.mlir"
      provides: "fixture for replacing the final EmitC output slot with LLVM-like HIVM text"
      min_lines: 20
    - path: "test/phase3/intrinsic_naming.mlir"
      provides: "fixture for op/type/variant-driven intrinsic-name selection"
      min_lines: 20
    - path: "test/phase3/unresolved_report.mlir"
      provides: "fixture for placeholder call shape plus unresolved sidecar content"
      min_lines: 20
    - path: "test/phase3/abs_emit.mlir"
      provides: "fixture for full module shape and llvm-as parseability on the Abs subset"
      min_lines: 25
    - path: "test/phase3/run_phase3_checks.sh"
      provides: "documented runner for all Phase 3 checks including llvm-as validation"
      min_lines: 20
    - path: ".planning/phases/03-hivm-emission/03-VALIDATION.md"
      provides: "updated validation contract with a fast quick-check path and closed Wave 0 markers"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "test/phase3/run_phase3_checks.sh"
      to: "test/phase3/abs_emit.mlir"
      via: "temporary output file passed through llvm-as and FileCheck"
      pattern: "llvm-as .*abs_emit\\.mlir|abs_emit\\.mlir"
    - from: "test/phase3/unresolved_report.mlir"
      to: "./build/tools/ptoas/ptoas"
      via: "explicit unresolved-report flag and sidecar assertions"
      pattern: "hivm-unresolved-report|__ptoas_hivm_unresolved"
---

<objective>
Create the Phase 3 validation fixtures and runner before implementation work begins.

Purpose: Replace the current Wave 0 gaps with committed source tests that define the exact HIVM text, intrinsic-name synthesis, unresolved placeholder, and parseability behaviors required for the final emission boundary.
Output: Four `test/phase3/*.mlir` fixtures plus one executable `run_phase3_checks.sh` helper.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hivm-emission/03-CONTEXT.md
@.planning/phases/03-hivm-emission/03-RESEARCH.md
@.planning/phases/03-hivm-emission/03-VALIDATION.md
@.planning/codebase/TESTING.md
@tools/ptoas/ptoas.cpp
@lib/PTO/Transforms/PTOToEmitC.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 3 MLIR/FileCheck fixtures</name>
  <read_first>
    test/phase3/backend_switch.mlir
    test/phase3/intrinsic_naming.mlir
    test/phase3/unresolved_report.mlir
    test/phase3/abs_emit.mlir
    .planning/phases/03-hivm-emission/03-CONTEXT.md
    .planning/phases/03-hivm-emission/03-RESEARCH.md
    .planning/phases/03-hivm-emission/03-VALIDATION.md
    tools/ptoas/ptoas.cpp
    lib/PTO/Transforms/PTOToEmitC.cpp
  </read_first>
  <files>test/phase3/backend_switch.mlir, test/phase3/intrinsic_naming.mlir, test/phase3/unresolved_report.mlir, test/phase3/abs_emit.mlir</files>
  <action>Create four new source tests under `test/phase3/` with `// RUN:` lines that use `./build/tools/ptoas/ptoas --pto-backend=a5vm` and `FileCheck`. In `backend_switch.mlir`, check that the final backend writes LLVM-like `.ll` text instead of EmitC/C++ by matching `; ModuleID =`, one `define void @...`, at least one `declare` or `call` line for an HIVM callee, and `CHECK-NOT: emitc.call_opaque`, `CHECK-NOT: __ubuf__`, and `CHECK-NOT: PTOAS__`. In `intrinsic_naming.mlir`, include one load-like case, one unary `a5vm.abs` case, and one store-like case whose output or debug trace proves names are synthesized from family plus type/variant fragments rather than one hardcoded `Abs` string path; match distinct spellings such as `vld`, `vabs`, and `vst` families together with vector-shape/type fragments and one suffix-bearing variant. Drive that fixture with `--dump-a5vm-ir` so the executor can check both the A5VM source attrs and the final selected names. In `unresolved_report.mlir`, force one unresolved family-selection case and assert that the main output still contains a structured placeholder callee beginning with `@__ptoas_hivm_unresolved.` while the sidecar contains the source op name, placeholder or candidate name, used naming fields, missing fields, and a location string. In `abs_emit.mlir`, model the implemented `Abs` subset and check for a full module shape with `; ModuleID =`, deduplicated `declare` lines, a `define void @abs...` function, LLVM vector types like `<64 x float>`, real `call` sites, and no stray `a5vm.` syntax in the main output. Keep the placeholder convention machine-distinguishable and non-reserved; do not use fake `llvm.` placeholder names.</action>
  <acceptance_criteria>
    rg -n '; ModuleID =|CHECK-NOT: emitc\\.call_opaque|CHECK-NOT: PTOAS__' test/phase3/backend_switch.mlir
    rg -n 'vld|vabs|vst|dump-a5vm-ir|CHECK-NOT: hardcoded Abs string' test/phase3/intrinsic_naming.mlir
    rg -n '__ptoas_hivm_unresolved|used_fields|missing_fields|loc' test/phase3/unresolved_report.mlir
    rg -n '; ModuleID =|define void @abs|<64 x float>|CHECK-NOT: a5vm\\.' test/phase3/abs_emit.mlir
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f test/phase3/backend_switch.mlir &amp;&amp; test -f test/phase3/intrinsic_naming.mlir &amp;&amp; test -f test/phase3/unresolved_report.mlir &amp;&amp; test -f test/phase3/abs_emit.mlir &amp;&amp; rg -n "RUN: .*--pto-backend=a5vm" test/phase3/*.mlir'</automated>
  </verify>
  <done>The repository contains four Phase 3 fixtures whose contents lock the exact final-emission boundary, intrinsic-name synthesis behavior, unresolved placeholder policy, and Abs-subset module shape required by HIVM-01 through HIVM-03.</done>
</task>

<task type="auto">
  <name>Task 2: Add a documented runner for all Phase 3 checks</name>
  <read_first>
    test/phase3/run_phase3_checks.sh
    .planning/phases/03-hivm-emission/03-VALIDATION.md
    test/phase2/run_phase2_checks.sh
  </read_first>
  <files>test/phase3/run_phase3_checks.sh, .planning/phases/03-hivm-emission/03-VALIDATION.md</files>
  <action>Create `test/phase3/run_phase3_checks.sh` as an executable Bash script with `#!/usr/bin/env bash` and `set -euo pipefail`. Resolve the compiler binary as `./build/tools/ptoas/ptoas` and fail with `error: missing ./build/tools/ptoas/ptoas` if absent. Use the local LLVM assembler path from the validation strategy: `/data/mouliangyu/projects/github.com/llvm/llvm-project/install/bin/llvm-as`. Run these checks in order, printing `phase3 check: <fixture>` before each compiler invocation: `backend_switch.mlir` through `ptoas --pto-backend=a5vm -o - | FileCheck`, `intrinsic_naming.mlir` through `ptoas --pto-backend=a5vm --dump-a5vm-ir -o - 2>&1 | FileCheck`, `unresolved_report.mlir` through a temporary unresolved-report path plus `FileCheck` on both stdout and the report file, `abs_emit.mlir` through a temporary output file that is parsed with `llvm-as` and then checked with `FileCheck`, and finally `ctest --test-dir build --output-on-failure`. In the same task, update `.planning/phases/03-hivm-emission/03-VALIDATION.md` so the quick-loop command becomes a sub-30 second smoke check such as the `backend_switch.mlir` fixture, the full-suite command remains the runner plus `ctest`, and the frontmatter is advanced to `wave_0_complete: true` and `nyquist_compliant: true` once the committed runner and fixtures exist. Keep the script as a direct invocation path only; do not add lit discovery or watch mode.</action>
  <acceptance_criteria>
    rg -n 'set -euo pipefail|missing \\./build/tools/ptoas/ptoas|phase3 check:' test/phase3/run_phase3_checks.sh
    rg -n 'backend_switch\\.mlir|intrinsic_naming\\.mlir|unresolved_report\\.mlir|abs_emit\\.mlir' test/phase3/run_phase3_checks.sh
    rg -n 'dump-a5vm-ir|hivm-unresolved-report|llvm-as|ctest --test-dir build --output-on-failure' test/phase3/run_phase3_checks.sh
    rg -n 'nyquist_compliant: true|wave_0_complete: true|backend_switch\\.mlir|Estimated runtime|~30 seconds' .planning/phases/03-hivm-emission/03-VALIDATION.md
    test -x test/phase3/run_phase3_checks.sh
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -x test/phase3/run_phase3_checks.sh &amp;&amp; rg -n "phase3 check:|FileCheck test/phase3/" test/phase3/run_phase3_checks.sh &amp;&amp; rg -n "dump-a5vm-ir|hivm-unresolved-report|llvm-as|ctest --test-dir build --output-on-failure" test/phase3/run_phase3_checks.sh &amp;&amp; rg -n "nyquist_compliant: true|wave_0_complete: true|backend_switch\\.mlir|~30 seconds" .planning/phases/03-hivm-emission/03-VALIDATION.md'</automated>
  </verify>
  <done>Phase 3 has a committed, executable verification entrypoint that covers final-emission replacement, naming, unresolved reporting, llvm-as parsing, and build-tree smoke checks without relying on external lit configuration, and the validation contract is updated to reflect a closed Wave 0 plus a fast smoke loop.</done>
</task>

</tasks>

<verification>
`bash -lc 'test -x test/phase3/run_phase3_checks.sh && rg -n "FileCheck test/phase3/" test/phase3/run_phase3_checks.sh && rg -n "dump-a5vm-ir|hivm-unresolved-report|llvm-as|ctest --test-dir build --output-on-failure" test/phase3/run_phase3_checks.sh && rg -n "nyquist_compliant: true|wave_0_complete: true|~30 seconds" .planning/phases/03-hivm-emission/03-VALIDATION.md && rg -n "RUN: .*--pto-backend=a5vm" test/phase3/*.mlir'`
</verification>

<success_criteria>
Every Phase 3 requirement in the roadmap has a corresponding committed source fixture, and the repository contains one executable runner that spells out the exact verification commands for the wave, including unresolved-sidecar assertions and local llvm-as parsing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hivm-emission/03-hivm-emission-01-SUMMARY.md`
</output>
