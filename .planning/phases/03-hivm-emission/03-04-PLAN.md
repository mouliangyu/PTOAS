---
phase: 03-hivm-emission
plan: 04
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - include/PTO/Transforms/Passes.h
  - include/PTO/Transforms/Passes.td
  - tools/ptoas/ptoas.cpp
autonomous: true
requirements:
  - HIVM-01
  - HIVM-02
  - HIVM-03
must_haves:
  truths:
    - "Running `ptoas --pto-backend=a5vm` emits LLVM-like HIVM text through the new final branch instead of EmitC/C++ for the implemented subset."
    - "Developer-facing debug/report flags are explicit, keep stdout reserved for the main IR artifact, and send diagnostics to stderr or a sidecar file."
    - "The Abs subset and unresolved placeholder behavior remain inspectable through the same CLI entrypoint without redesigning the pass pipeline."
  artifacts:
    - path: "tools/ptoas/ptoas.cpp"
      provides: "final emission-branch replacement and CLI flag wiring"
      contains: "translateA5VMModuleToText"
    - path: "include/PTO/Transforms/Passes.h"
      provides: "declaration surface aligned with the final HIVM emission entrypoint"
      contains: "A5VMTextEmitter"
    - path: "include/PTO/Transforms/Passes.td"
      provides: "pass registration alignment if the final branch needs new options or declarations"
      contains: "Pass"
  key_links:
    - from: "tools/ptoas/ptoas.cpp"
      to: "include/PTO/Transforms/A5VMTextEmitter.h"
      via: "translateA5VMModuleToText call in the backend=a5vm final branch"
      pattern: "translateA5VMModuleToText"
    - from: "tools/ptoas/ptoas.cpp"
      to: "test/phase3/run_phase3_checks.sh"
      via: "CLI flags exercised by the committed Wave 0 runner"
      pattern: "dump-a5vm-ir|hivm-unresolved-report|pto-backend"
---

<objective>
Wire the new HIVM text emitter into `ptoas` and replace the final EmitC output dependency for the implemented subset.

Purpose: Finish the phase at the real tool boundary by swapping the current EmitC-only final output branch for the new A5VM-to-HIVM text path while preserving the existing pass pipeline shape.
Output: Updated CLI flags, the final `ptoas` emission branch, and end-to-end execution of the committed Phase 3 checks.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hivm-emission/03-CONTEXT.md
@.planning/phases/03-hivm-emission/03-RESEARCH.md
@.planning/phases/03-hivm-emission/03-VALIDATION.md
@.planning/phases/03-hivm-emission/03-02-PLAN.md
@.planning/phases/03-hivm-emission/03-03-PLAN.md
@include/PTO/Transforms/Passes.h
@include/PTO/Transforms/Passes.td
@tools/ptoas/ptoas.cpp
@test/phase3/run_phase3_checks.sh

<interfaces>
From `include/PTO/Transforms/A5VMTextEmitter.h`:
```c++
struct A5VMEmissionOptions;
LogicalResult translateA5VMModuleToText(ModuleOp module,
                                        llvm::raw_ostream &irOS,
                                        llvm::raw_ostream &diagOS,
                                        const A5VMEmissionOptions &options);
```

From `include/PTO/Transforms/Passes.h`:
```c++
std::unique_ptr<Pass> createEmitPTOManualPass(PTOArch arch);
std::unique_ptr<Pass> createConvertToPTOOpPass();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the final EmitC translation slot for backend=a5vm</name>
  <read_first>
    tools/ptoas/ptoas.cpp
    include/PTO/Transforms/Passes.h
    include/PTO/Transforms/Passes.td
    include/PTO/Transforms/A5VMTextEmitter.h
    .planning/phases/03-hivm-emission/03-CONTEXT.md
    .planning/phases/03-hivm-emission/03-RESEARCH.md
  </read_first>
  <files>tools/ptoas/ptoas.cpp, include/PTO/Transforms/Passes.h, include/PTO/Transforms/Passes.td</files>
  <action>Update `tools/ptoas/ptoas.cpp` so `--pto-backend=a5vm` keeps the shared pre-backend pipeline shape already established, bypasses the final EmitC translation/output path, and calls `translateA5VMModuleToText(...)` to write the final `.ll`-style artifact to the normal `-o` destination. Keep the legacy `emitc` backend path unchanged. Add explicit developer-facing flags for the Phase 3 workflow, including `--dump-a5vm-ir` and `--hivm-unresolved-report=<path>`; keep debug/trace output on `stderr` so stdout or `-o` stays clean. If additional declarations are needed in `Passes.h` or `Passes.td`, keep them minimal and aligned with the existing pass-registration surface; do not redesign the pass topology or invent a separate tool.</action>
  <acceptance_criteria>
    rg -n 'pto-backend|dump-a5vm-ir|hivm-unresolved-report|translateA5VMModuleToText' tools/ptoas/ptoas.cpp
    rg -n 'emitc::translateToCpp|translateA5VMModuleToText' tools/ptoas/ptoas.cpp
    rg -n 'A5VMTextEmitter|hivm' include/PTO/Transforms/Passes.h include/PTO/Transforms/Passes.td
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'ninja -C build PTOTransforms ptoas &amp;&amp; ./build/tools/ptoas/ptoas --pto-backend=a5vm test/phase3/backend_switch.mlir -o - | FileCheck test/phase3/backend_switch.mlir'</automated>
  </verify>
  <done>`ptoas --pto-backend=a5vm` now uses the new text emitter at the final output seam, while the old EmitC branch remains available and the new debug/report flags are explicit and channel-separated.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full Phase 3 validation loop through the real CLI entrypoint</name>
  <read_first>
    test/phase3/run_phase3_checks.sh
    test/phase3/backend_switch.mlir
    test/phase3/intrinsic_naming.mlir
    test/phase3/unresolved_report.mlir
    test/phase3/abs_emit.mlir
  </read_first>
  <files>tools/ptoas/ptoas.cpp</files>
  <action>Use the committed Phase 3 runner as the final verification path. Ensure the tool wiring satisfies all four fixtures exactly: final branch replacement, family/type/variant-driven naming, unresolved placeholder plus sidecar reporting, and llvm-as parseability for the Abs subset. If any verification issue reveals a mismatch between the CLI flags and the Wave 0 fixtures, adjust `ptoas.cpp` to match the committed validation contract instead of weakening the fixtures. Preserve the realistic path for the Abs subset and keep unresolved placeholders present in the main output rather than dropping unsupported sites.</action>
  <acceptance_criteria>
    bash test/phase3/run_phase3_checks.sh
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'bash test/phase3/run_phase3_checks.sh'</automated>
  </verify>
  <done>The real `ptoas` entrypoint passes the full Phase 3 validation suite, proving that the final EmitC output dependency is removed for the implemented subset and that unresolved placeholder behavior remains visible and reportable.</done>
</task>

</tasks>

<verification>
`bash -lc 'ninja -C build PTOTransforms ptoas && bash test/phase3/run_phase3_checks.sh'`
</verification>

<success_criteria>
Running `ptoas --pto-backend=a5vm` now emits LLVM-like HIVM text for the implemented subset, the naming and unresolved-report policies are observable through explicit flags, and the committed Phase 3 suite passes through the real CLI boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hivm-emission/03-hivm-emission-04-SUMMARY.md`
</output>
